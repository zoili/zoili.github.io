<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>cmake文件里面的命令、环境变量、属性、变量、生成器以及编译功能特征</title>
      <link href="/2023/01/04/cmakedesc2/"/>
      <url>/2023/01/04/cmakedesc2/</url>
      
        <content type="html"><![CDATA[<h1 id="cmake文件里面的命令、环境变量、属性、变量、生成器以及编译功能特征-compile-features"><a href="#cmake文件里面的命令、环境变量、属性、变量、生成器以及编译功能特征-compile-features" class="headerlink" title="cmake文件里面的命令、环境变量、属性、变量、生成器以及编译功能特征(compile_features)"></a>cmake文件里面的命令、环境变量、属性、变量、生成器以及编译功能特征(compile_features)</h1><p>参考：<br>官方文档和官方教程：<br><a href="https://cmake.org/cmake/help/latest/index.html">https://cmake.org/cmake/help/latest/index.html</a><br><a href="https://cmake.org/cmake/help/latest/guide/tutorial/index.html">https://cmake.org/cmake/help/latest/guide/tutorial/index.html</a></p><h2 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h2><p>1.先自己跟着教程走一遍，有个大概了解  </p><p><img src="/images/cmakedesccomm1.jpg"></p><p>2.命令行–这里只讲解cmake命令，ctest和cpack都比较简单，自己去官网上看看就好<br><img src="/images/cmakedesccomm2.jpg"></p><p>3.其他，比如构建系统、cmake里面的命令、cmake内部属性变量、cmake生成器表达式、cmake语法等等<br>只讲如下标红框的内容</p><p><img src="/images/cmakedesccomm3.jpg"></p><h2 id="cmake文件里面的命令"><a href="#cmake文件里面的命令" class="headerlink" title="cmake文件里面的命令"></a>cmake文件里面的命令</h2><p>具体看链接<br><a href="https://cmake.org/cmake/help/latest/manual/cmake-commands.7.html">https://cmake.org/cmake/help/latest/manual/cmake-commands.7.html</a><br><img src="/images/cmakedesc2.jpg"><br><img src="/images/cmakedesc3.jpg"><br><img src="/images/cmakedesc4.jpg"></p><h2 id="cmake文件里面的环境变量"><a href="#cmake文件里面的环境变量" class="headerlink" title="cmake文件里面的环境变量"></a>cmake文件里面的环境变量</h2><p>具体看链接<br><a href="https://cmake.org/cmake/help/latest/manual/cmake-env-variables.7.html">https://cmake.org/cmake/help/latest/manual/cmake-env-variables.7.html</a><br><img src="/images/cmakedesc5.jpg"><br><img src="/images/cmakedesc6.jpg"><br><img src="/images/cmakedesc7.jpg"></p><h2 id="cmake文件里面的属性"><a href="#cmake文件里面的属性" class="headerlink" title="cmake文件里面的属性"></a>cmake文件里面的属性</h2><p>具体看链接<br><a href="https://cmake.org/cmake/help/latest/manual/cmake-properties.7.html">https://cmake.org/cmake/help/latest/manual/cmake-properties.7.html</a><br><img src="/images/cmakedesc10.jpg"><br><img src="/images/cmakedesc11.jpg"><br><img src="/images/cmakedesc12.jpg"></p><h2 id="cmake文件里面的变量"><a href="#cmake文件里面的变量" class="headerlink" title="cmake文件里面的变量"></a>cmake文件里面的变量</h2><p>具体看链接<br><a href="https://cmake.org/cmake/help/latest/manual/cmake-variables.7.html">https://cmake.org/cmake/help/latest/manual/cmake-variables.7.html</a><br><img src="/images/cmakedesc13.jpg"><br><img src="/images/cmakedesc14.jpg"><br><img src="/images/cmakedesc15.jpg"></p><h2 id="cmake生成器"><a href="#cmake生成器" class="headerlink" title="cmake生成器"></a>cmake生成器</h2><p>具体看链接<br><a href="https://cmake.org/cmake/help/latest/manual/cmake-generators.7.html">https://cmake.org/cmake/help/latest/manual/cmake-generators.7.html</a></p><p>CMake 生成器负责为本机构建系统编写输入文件。必须为构建树选择一个CMake 生成器，以确定要使用的本机构建系统。可选地，可以选择其中一个额外生成器作为某些命令行构建工具生成器的变体，以生成用于辅助 IDE 的项目文件。</p><p>CMake 生成器是特定于平台的，因此每个生成器可能仅在特定平台上可用。这cmake(1)命令行工具 –help输出列出了当前平台上可用的生成器。使用它的-G选项为新的构建树指定生成器。这cmake-gui(1)在创建新的构建树时提供生成器的交互式选择。</p><p><img src="/images/cmakedesc8.jpg"><br><img src="/images/cmakedesc9.jpg"></p><h2 id="cmake编译功能特征-compile-features"><a href="#cmake编译功能特征-compile-features" class="headerlink" title="cmake编译功能特征(compile_features)"></a>cmake编译功能特征(compile_features)</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>项目源代码可能取决于或取决于编译器某些功能的可用性。出现了三个用例： 编译功能要求、可选编译功能 和条件编译选项。</p><p>虽然功能通常在编程语言标准中指定，但 CMake 提供了一个基于对功能的精细处理的主要用户界面，而不是引入该功能的语言标准。</p><p>这CMAKE_C_KNOWN_FEATURES,CMAKE_CUDA_KNOWN_FEATURES， 和CMAKE_CXX_KNOWN_FEATURES全局属性包含 CMake 已知的所有功能，无论编译器是否支持该功能。这CMAKE_C_COMPILE_FEATURES,CMAKE_CUDA_COMPILE_FEATURES ， 和CMAKE_CXX_COMPILE_FEATURES变量包含 CMake 知道的编译器已知的所有功能，无论使用它们所需的语言标准或编译标志如何。</p><p>CMake 已知的功能命名大多遵循与 Clang 功能测试宏相同的约定。有一些例外，例如 CMake 使用cxx_finaland而不是Clang 使用cxx_override的 single 。cxx_override_control</p><p>OBJC请注意，对于或OBJCXX语言没有单独的编译特性属性或变量。它们分别基于 offC或C++ ，因此应该使用它们相应的基本语言的属性和变量。</p><h3 id="编译特性要求"><a href="#编译特性要求" class="headerlink" title="编译特性要求"></a>编译特性要求</h3><p>可以指定编译功能要求 target_compile_features()命令。例如，如果必须使用编译器支持编译目标 cxx_constexpr特征：</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>mylib requires_constexpr.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_features</span><span class="token punctuation">(</span>mylib <span class="token namespace">PRIVATE</span> <span class="token property">cxx_constexpr</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>在处理cxx_constexpr功能需求时， cmake(1)将确保正在使用的 C++ 编译器具有该功能，并将添加任何必要的标志，例如目标-std=gnu++11 中 C++ 文件的编译行mylib。FATAL_ERROR如果编译器不支持该功能，则会发出A。</p><p>确切的编译标志和语言标准故意不包含在该用例的用户界面中。CMake 将通过考虑为每个目标指定的功能来计算要使用的适当编译标志。</p><p>即使编译器支持没有标志的特定功能，也会添加此类编译标志。例如，即使-std=gnu++98使用了 GNU 编译器也支持可变参数模板（带有警告）。 如果指定为要求，则CMake 添加-std=gnu++11标志。cxx_variadic_templates</p><p>在上面的例子中，当它自己构建时mylib需要cxx_constexpr，但消费者mylib不需要使用支持cxx_constexpr的编译器。如果 mylib的接口 确实需要该功能cxx_constexpr（或任何其他已知功能），则可以使用PUBLIC或INTERFACE 的签名指定target_compile_features():</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>mylib requires_constexpr.cpp<span class="token punctuation">)</span><span class="token comment"># cxx_constexpr is a usage-requirement</span><span class="token keyword">target_compile_features</span><span class="token punctuation">(</span>mylib <span class="token namespace">PUBLIC</span> <span class="token property">cxx_constexpr</span><span class="token punctuation">)</span><span class="token comment"># main.cpp will be compiled with -std=gnu++11 on GNU for cxx_constexpr.</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myexe main.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myexe mylib<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>通过使用链接实现来传递地评估功能需求。看cmake-buildsystem(7)有关构建属性和使用要求的传递行为的更多信息。</p><h3 id="要求语言标准"><a href="#要求语言标准" class="headerlink" title="要求语言标准"></a>要求语言标准</h3><p>在使用来自特定语言标准（例如 C++ 11）的大量常用功能的项目中，可以指定一个元功能（例如cxx_std_11），该功能需要使用至少了解该标准的编译器模式，但可以更大。这比单独指定所有特征更简单，但不保证任何特定特征的存在。使用不受支持的功能的诊断将延迟到编译时。</p><p>例如，如果项目的头文件中大量使用 C++ 11 特性，则客户端必须使用不低于 C++ 11 的编译器模式。可以通过代码来请求：</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_compile_features</span><span class="token punctuation">(</span>mylib <span class="token namespace">PUBLIC</span> <span class="token property">cxx_std_11</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>在此示例中，CMake 将确保以至少 C++ 11（或 C++ 14、C++ 17 等）的模式调用编译器，并 -std=gnu++11在必要时添加标志。这适用于内部的来源mylib 以及任何依赖项（可能包括来自 的标题mylib）。</p><p>笔记 如果编译器的默认标准级别至少是所请求功能的标准级别，CMake 可能会忽略该-std=标志。如果编译器的默认扩展模式与<LANG>_EXTENSIONS目标属性，或者如果<LANG>_STANDARD目标属性已设置。</p><h3 id="编译器扩展的可用性"><a href="#编译器扩展的可用性" class="headerlink" title="编译器扩展的可用性"></a>编译器扩展的可用性</h3><p>这&lt;LANG&gt;_EXTENSIONStarget 属性默认为编译器的默认值（请参阅CMAKE_&lt;LANG&gt;_EXTENSIONS_DEFAULT). 请注意，由于大多数编译器默认启用扩展，这可能会暴露用户代码或第三方依赖项标头中的可移植性错误。</p><p>&lt;LANG&gt;_EXTENSIONS用于默认为ON. 看CMP0128.</p><h3 id="可选的编译特性"><a href="#可选的编译特性" class="headerlink" title="可选的编译特性"></a>可选的编译特性</h3><p>如果可用，编译功能可能是首选，而不会产生硬性要求。这可以通过不指定功能 来实现target_compile_features()而是在项目代码中使用预处理器条件检查编译器功能。</p><p>在此用例中，项目可能希望建立一个特定的语言标准（如果编译器可用），并使用预处理器条件来检测实际可用的功能。语言标准可以通过使用要求语言标准来 建立target_compile_features()元功能，如cxx_std_11，或通过设置CXX_STANDARD目标财产或 CMAKE_CXX_STANDARD多变的。</p><p>另请参阅政策CMP0120以及关于已弃用的 示例用法的遗留文档 WriteCompilerDetectionHeader模块。</p><h3 id="条件编译选项"><a href="#条件编译选项" class="headerlink" title="条件编译选项"></a>条件编译选项</h3><p>库可能会根据请求的编译器功能提供完全不同的头文件。</p><p>例如，标头 atwith_variadics/interface.h可能包含：</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">template</span><span class="token operator">&lt;</span><span class="token keyword">int</span> I<span class="token punctuation">,</span> <span class="token keyword">int</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> Is<span class="token operator">></span><span class="token keyword">struct</span> <span class="token class-name">Interface</span><span class="token punctuation">;</span><span class="token keyword">template</span><span class="token operator">&lt;</span><span class="token keyword">int</span> I<span class="token operator">></span><span class="token keyword">struct</span> <span class="token class-name">Interface</span><span class="token operator">&lt;</span>I<span class="token operator">></span><span class="token punctuation">&#123;</span>  <span class="token keyword">static</span> <span class="token keyword">int</span> <span class="token function">accumulate</span><span class="token punctuation">(</span><span class="token punctuation">)</span>  <span class="token punctuation">&#123;</span>    <span class="token keyword">return</span> I<span class="token punctuation">;</span>  <span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span><span class="token keyword">template</span><span class="token operator">&lt;</span><span class="token keyword">int</span> I<span class="token punctuation">,</span> <span class="token keyword">int</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> Is<span class="token operator">></span><span class="token keyword">struct</span> <span class="token class-name">Interface</span><span class="token punctuation">&#123;</span>  <span class="token keyword">static</span> <span class="token keyword">int</span> <span class="token function">accumulate</span><span class="token punctuation">(</span><span class="token punctuation">)</span>  <span class="token punctuation">&#123;</span>    <span class="token keyword">return</span> I <span class="token operator">+</span> <span class="token class-name">Interface</span><span class="token operator">&lt;</span>Is<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token operator">></span><span class="token double-colon punctuation">::</span><span class="token function">accumulate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>而标题no_variadics/interface.h可能包含：</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">template</span><span class="token operator">&lt;</span><span class="token keyword">int</span> I1<span class="token punctuation">,</span> <span class="token keyword">int</span> I2 <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token keyword">int</span> I3 <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token keyword">int</span> I4 <span class="token operator">=</span> <span class="token number">0</span><span class="token operator">></span><span class="token keyword">struct</span> <span class="token class-name">Interface</span><span class="token punctuation">&#123;</span>  <span class="token keyword">static</span> <span class="token keyword">int</span> <span class="token function">accumulate</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span> <span class="token keyword">return</span> I1 <span class="token operator">+</span> I2 <span class="token operator">+</span> I3 <span class="token operator">+</span> I4<span class="token punctuation">;</span> <span class="token punctuation">&#125;</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以编写interface.h包含以下内容的抽象标头：</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">ifdef</span> <span class="token expression">HAVE_CXX_VARIADIC_TEMPLATES</span></span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"with_variadics/interface.h"</span></span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">else</span></span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"no_variadics/interface.h"</span></span><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">endif</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>然而，如果有很多文件要抽象，这可能无法维护。需要的是根据编译器功能使用替代包含目录。</p><p>CMake 提供了一个COMPILE_FEATURES generator expression实施这些条件。这可以与 build-property 命令一起使用，例如 target_include_directories()和target_link_libraries() 设置适当的buildsystem 特性：</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>foo <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">set</span><span class="token punctuation">(</span>with_variadics <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_SOURCE_DIR</span><span class="token punctuation">&#125;</span>/with_variadics<span class="token punctuation">)</span><span class="token keyword">set</span><span class="token punctuation">(</span>no_variadics <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_SOURCE_DIR</span><span class="token punctuation">&#125;</span>/no_variadics<span class="token punctuation">)</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>foo  <span class="token namespace">INTERFACE</span>    <span class="token string">"$&lt;$&lt;COMPILE_FEATURES:cxx_variadic_templates>:<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">with_variadics</span><span class="token punctuation">&#125;</span></span>>"</span>    <span class="token string">"$&lt;$&lt;NOT:$&lt;COMPILE_FEATURES:cxx_variadic_templates>>:<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">no_variadics</span><span class="token punctuation">&#125;</span></span>>"</span>  <span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>使用代码然后foo像往常一样简单地链接到目标并使用适合特性的包含目录</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_executable</span><span class="token punctuation">(</span>consumer_with consumer_with.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>consumer_with foo<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET consumer_with <span class="token property">CXX_STANDARD</span> <span class="token number">11</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>consumer_no consumer_no.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>consumer_no foo<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="支持的编译器"><a href="#支持的编译器" class="headerlink" title="支持的编译器"></a>支持的编译器</h3><p>CMake 目前知道C++ standards 和compile features可从以下compiler ids从为每个指定的版本开始：</p><p>AppleClang: 适用于 Xcode 版本 4.4+ 的 Apple Clang。</p><p>Clang: Clang 编译器版本 2.9+。</p><p>GNU：GNU 编译器版本 4.4+。</p><p>MSVC: 微软 Visual Studio 版本 2010+。</p><p>SunPro：Oracle SolarisStudio 版本 12.4+。</p><p>Intel：英特尔编译器版本 12.1+。</p><p>CMake 目前知道C standards 和compile features可从以下compiler ids从为每个指定的版本开始：</p><p>上面列出的所有 C++ 编译器和版本。</p><p>GNU: GNU 编译器版本 3.4+</p><p>CMake 目前知道C++ standards及其关联的元功能（例如cxx_std_11）可从以下位置获得compiler ids从为每个指定的版本开始：</p><p>Cray: Cray 编译环境版本 8.1+。</p><p>Fujitsu: 富士通 HPC 编译器 4.0+。</p><p>PGI：PGI 版本 12.10+。</p><p>NVHPC：NVIDIA HPC 编译器版本 11.0+。</p><p>TI：德州仪器编译器。</p><p>XL：IBM XL 版本 10.1+。</p><p>CMake 目前知道C standards及其关联的元功能（例如c_std_99）可从以下位置获得compiler ids从为每个指定的版本开始：</p><p>上面列出的所有编译器和版本仅具有 C++ 的元功能。</p><p>CMake 目前知道CUDA standards及其关联的元功能（例如cuda_std_11）可从以下位置获得compiler ids从为每个指定的版本开始：</p><p>Clang: Clang 编译器 5.0+。</p><p>NVIDIA: NVIDIA nvcc 编译器 7.5+。</p>]]></content>
      
      
      <categories>
          
          <category> 编译安装 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>cmake基本使用（命令行）</title>
      <link href="/2022/12/16/cmakedesc/"/>
      <url>/2022/12/16/cmakedesc/</url>
      
        <content type="html"><![CDATA[<h1 id="cmake基本使用（命令行）"><a href="#cmake基本使用（命令行）" class="headerlink" title="cmake基本使用（命令行）"></a>cmake基本使用（命令行）</h1><p>参考：<br>官方文档和官方教程：<br><a href="https://cmake.org/cmake/help/latest/index.html">https://cmake.org/cmake/help/latest/index.html</a><br><a href="https://cmake.org/cmake/help/latest/guide/tutorial/index.html">https://cmake.org/cmake/help/latest/guide/tutorial/index.html</a></p><h2 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h2><p>1.先自己跟着教程走一遍，有个大概了解  </p><p><img src="/images/cmakedesccomm1.jpg"></p><p>2.命令行–这里只讲解cmake命令，ctest和cpack都比较简单，自己去官网上看看就好<br><img src="/images/cmakedesccomm2.jpg"></p><p>3.其他，比如构建系统、cmake里面的命令、cmake内部属性变量、cmake生成器表达式、cmake语法等等<br>只讲如下标红框的内容</p><p><img src="/images/cmakedesccomm3.jpg"></p><h2 id="cmake命令行"><a href="#cmake命令行" class="headerlink" title="cmake命令行"></a>cmake命令行</h2><h3 id="cmake"><a href="#cmake" class="headerlink" title="cmake"></a>cmake</h3><h4 id="生成一个项目构建系统"><a href="#生成一个项目构建系统" class="headerlink" title="生成一个项目构建系统"></a>生成一个项目构建系统</h4><p><strong>cmake [&lt;options&gt;] &lt;path-to-source&gt;</strong> </p><p>使用当前工作目录作为构建目录，使用path-to-source作为源代码目录。指定的路径可以是相对于当前工作目录的绝对路径或相对路径。源树必须包含一个CMakeLists.txt文件，不能包含一个CMakeCache.txt文件，因为后者标识了一个现有的构建目录  。</p><pre class="line-numbers language-none"><code class="language-none">mkdir build ; cd build  cmake ..&#x2F;src<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>cmake [&lt;options&gt;] &lt;path-to-existing-build&gt;</strong></p><p>使用ath-to-existing-build作为构建目录，并从它的CMakeCache.txt文件中加载到源代码目录的路径，该文件必须已经由以前的CMake运行生成。指定的路径可以是相对于当前工作目录的绝对路径或相对路径  </p><pre class="line-numbers language-none"><code class="language-none">cd build  cmake .<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>cmake [&lt;options&gt;] -S &lt;path-to-source&gt; -B &lt;path-to-build&gt;</strong></p><p>使用 ath-to-build作为构建目录，使用 path-to-source作为源代码目录。指定的路径可以是相对于当前工作目录的绝对路径或相对路径。源树必须包含CMakeLists.txt文件。如果构建树不存在，将自动创建</p><pre class="line-numbers language-none"><code class="language-none">cmake -S src -B build<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><img src="/images/cmakedesc1.jpg"></p><p>在生成一个构建系统之后，可以使用相应的本地构建工具来构建项目。例如，在使用Unix Makefiles生成器后，可以直接运行make</p><pre class="line-numbers language-none"><code class="language-none">makemake install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p><strong>Options</strong></p><p>讲几个重要的，其他的可以看官网文档</p><p><strong>-S &lt;path-to-source&gt;</strong><br>要构建的CMake项目的根目录的路径</p><p><strong>-B &lt;path-to-build&gt;</strong><br>到CMake将用作构建目录根的目录的路径。<br>如果目录不存在，CMake将创建它。</p><p><strong>-C &lt;initial-cache&gt;</strong><br>预加载脚本以填充缓存。</p><p>当CMake第一次在一个空的构建树中运行时，它会创建一个CMakeCache.txt文件，并为项目填充可定制的设置。此选项可用于指定在第一次通过项目的CMake列表文件之前从其中加载缓存项的文件。加载的条目优先于项目的默认值。给定的文件应该是一个CMake脚本，其中包含使用CACHE选项的set()命令，而不是缓存格式的文件。</p><p>脚本中对CMAKE_SOURCE_DIR和CMAKE_BINARY_DIR的引用计算为顶级源代码和构建目录</p><p><strong>-D &lt;var&gt;:&lt;type&gt;=&lt;value&gt;, -D &lt;var&gt;=&lt;value&gt;</strong><br>创建或更新CMake CACHE条目。</p><p>当CMake第一次在一个空的构建树中运行时，它会创建一个CMakeCache.txt文件，并为项目填充可定制的设置。此选项可用于指定优先于项目默认值的设置。可以根据需要对任意数量的缓存项重复该选项。</p><p>如果给出了:type 部分，则它必须是set()命令文档为其CACHE签名指定的类型之一。如果:type 部分被省略，则如果条目不存在类型，则该条目将创建为没有类型的条目。如果项目中的命令将类型设置为PATH或FILEPATH，则 <value> 将被转换为绝对路径。</p><p>该选项也可以作为单个参数给出:-D&lt;var&gt;:&lt;type&gt;=&lt;value&gt; or -D&lt;var&gt;=&lt;value&gt;.</p><p>需要注意的是，-C和-D参数的顺序非常重要。它们将按照列出的顺序执行，最后一个参数优先于前面的参数。例如，如果你指定-DCMAKE_BUILD_TYPE=Debug，后面跟着一个-C参数，文件调用</p><pre class="line-numbers language-none"><code class="language-none">set(CMAKE_BUILD_TYPE &quot;Release&quot; CACHE STRING &quot;&quot; FORCE)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>那么-C参数将优先，CMAKE_BUILD_TYPE将被设置为Release。但是，如果-D参数出现在-C参数之后，它将被设置为Debug。</p><p>如果set(… CACHE …)调用在-C文件中不使用FORCE，并且-D参数设置了相同的变量，-D参数将优先考虑顺序</p><p><strong>-U &lt;globbing_expr&gt;</strong><br>从CMake CACHE中删除匹配的条目。</p><p>此选项可用于从CMakeCache.txt文件中删除一个或多个变量，使用*和?都受支持。可以根据需要对任意数量的缓存项重复该选项。</p><p>小心使用，你可以让你的CMakeCache.txt无法工作</p><p><strong>-G &lt;generator-name&gt;</strong></p><p>指定生成系统生成器。</p><p>CMake可能在某些平台上支持多个本地构建系统。生成器负责生成特定的构建系统。可能的生成器名称在cmake generators(7)<a href="https://cmake.org/cmake/help/latest/manual/cmake-generators.7.html#manual:cmake-generators(7)">https://cmake.org/cmake/help/latest/manual/cmake-generators.7.html#manual:cmake-generators(7)</a>手册中指定。</p><p>如果没有指定，CMake检查CMAKE_GENERATOR环境变量，否则退回到内置的默认选择</p><blockquote><p>大概有如下generator-name：<br>Borland Makefiles<br>MSYS Makefiles<br>MinGW Makefiles<br>NMake Makefiles<br>NMake Makefiles JOM<br>Unix Makefiles<br>Watcom WMake<br>Visual Studio 15 2017<br>Visual Studio 16 2019<br>Visual Studio 17 2022<br>…</p></blockquote><p><strong>-T &lt;toolset-spec&gt;</strong><br>生成器的工具集规范(如果支持的话)。</p><p>一些CMake生成器支持工具集规范，以告诉本机构建系统如何选择编译器。详细信息请参阅CMAKE_GENERATOR_TOOLSET变量。</p><p><strong>-L[A][H]</strong><br>列出非高级缓存变量。</p><p>列表缓存变量将运行CMake，并从CMake缓存中列出所有未标记为INTERNAL或ADVANCED的变量。这将有效地显示当前的CMake设置，然后可以用-D选项进行更改。改变一些变量可能会导致创建更多的变量。如果指定了A，那么它也会显示高级变量。如果指定了H，它还将显示每个变量的帮助</p><p><strong>–log-level=&lt;level&gt;</strong><br>设置日志 &lt;级别&gt; 。<br>message()命令将只输出指定日志级别或更高级别的消息。有效的日志级别为ERROR、WARNING、NOTICE、STATUS(默认)、VERBOSE、DEBUG和TRACE。</p><p>要使CMake运行之间的日志级别保持不变，请将CMAKE_MESSAGE_LOG_LEVEL设置为缓存变量。如果同时给出了命令行选项和变量，则命令行选项优先。</p><p>出于向后兼容性的考虑，——loglevel也可以作为该选项的同义词。</p><p><strong>–log-context</strong><br>启用message()命令输出附加到每个消息的上下文。</p><p>此选项只显示当前CMake运行的上下文。要使在所有后续CMake运行中显示上下文持久，请将CMAKE_MESSAGE_CONTEXT_SHOW设置为缓存变量。当给出这个命令行选项时，CMAKE_MESSAGE_CONTEXT_SHOW将被忽略。</p><p><strong>–debug-find</strong><br>将cmake find命令置于调试模式。</p><p>在cmake运行过程中打印额外的find调用信息到标准错误。输出是为人类使用而设计的，而不是用于解析。另请参阅CMAKE_FIND_DEBUG_MODE变量，以调试项目更本地的部分</p><p><strong>–debug-find-pkg=&lt;pkg&gt;[,…]</strong><br>在调用find_package( )时，将cmake find命令置于调试模式，其中 是给定的以逗号分隔的区分大小写的包名列表中的一个条目。</p><p>类似于——debug-find，但将作用域限制为指定的包</p><p><strong>–debug-find-var=&lt;var&gt;[,…]</strong><br>当使用作为结果变量调用时，将cmake find命令置于调试模式，其中是给定逗号分隔列表中的一个条目。</p><p>类似于——debug-find，但将作用域限制为指定的变量名。</p><p><strong>–preset &lt;preset&gt;, –preset=&lt;preset&gt;</strong><br>从 /CMakePresets中读取预设值。和 /CMakeUserPresets.json。预设可以指定生成器和构建目录，以及传递给CMake的变量和其他参数列表。当前工作目录必须包含CMake预设文件。CMake GUI也可以识别CMakePresets。json和CMakeUserPresets。json文件。有关这些文件的详细信息，请参见cmake-presets(7)。</p><p>在所有其他命令行选项之前读取预设。预置所指定的选项(变量、生成器等)都可以通过在命令行上手动指定来覆盖。例如，如果预设将一个名为MYVAR的变量设置为1，但用户使用-D参数将其设置为2，则首选值为2。</p><p><strong>–list-presets[=&lt;type&gt;]</strong><br>列出指定type的可用预设。 type的有效值为configure、build、test、package或all。如果省略 ，则假设为configure。当前工作目录必须包含CMake预设文件。</p><h4 id="创建一个项目"><a href="#创建一个项目" class="headerlink" title="创建一个项目"></a>创建一个项目</h4><p><strong>cmake –build &lt;dir&gt;             [&lt;options&gt;] [– &lt;build-tool-options&gt;]<br>cmake –build –preset &lt;preset&gt; [&lt;options&gt;] [– &lt;build-tool-options&gt;]</strong>  </p><p><strong>–build &lt;dir&gt;</strong><br>要构建的项目二进制目录。这是必需的(除非指定了预设)，并且必须是第一个</p><p><strong>–preset &lt;preset&gt;, –preset=&lt;preset&gt;</strong><br>使用生成预设来指定生成选项。项目二进制目录是从configurePreset键推断出来的。当前工作目录必须包含CMake预设文件。详细信息请参见预置。</p><p><strong>–list-presets</strong><br>列出可用的生成预设。当前工作目录必须包含CMake预设文件</p><p><strong>-j [&lt;jobs&gt;], –parallel [&lt;jobs&gt;]</strong><br>构建时使用的最大并发进程数。如果省略 ，则使用本机构建工具的默认编号。</p><p>如果设置了CMAKE_BUILD_PARALLEL_LEVEL环境变量，则在没有给出此选项时指定默认并行级别。</p><p>一些本地构建工具总是并行构建。 值为1的使用可用于限制单个作业。</p><p><strong>-t &lt;tgt&gt;…, –target &lt;tgt&gt;…</strong><br>Build 而不是默认目标。可以给出多个目标，用空格分隔</p><p><strong>–config &lt;cfg&gt;</strong><br>对于多配置工具，请选择configuration</p><p><strong>–clean-first</strong><br>首先构建目标清洁，然后构建 (To clean only, use –target clean.)</p><p><strong>-v, –verbose</strong><br>启用详细输出(如果支持)，包括要执行的构建命令。</p><p>如果设置了VERBOSE环境变量或CMAKE_VERBOSE_MAKEFILE缓存变量，则可以省略此选项</p><p><strong>–</strong><br>将剩余选项传递给本地编译工具</p><h4 id="安装项目"><a href="#安装项目" class="headerlink" title="安装项目"></a>安装项目</h4><p><strong>cmake –install &lt;dir&gt; [&lt;options&gt;]</strong>  </p><p><strong>–install &lt;dir&gt;</strong><br>要安装的项目二进制目录。这是必需的，必须放在第一位</p><p><strong>–config &lt;cfg&gt;</strong><br>对于多配置生成器，选择configuration </p><p><strong>–component &lt;comp&gt;</strong><br>基于组件的安装。只安装组件 </p><p><strong>–default-directory-permissions &lt;permissions&gt;</strong><br>默认目录安装权限。权限格式 &lt;u=rwx,g=rx,o=rx&gt;</p><p><strong>–prefix &lt;prefix&gt;</strong><br>重写安装前缀CMAKE_INSTALL_PREFIX</p><p><strong>–strip</strong><br>安装前先剥离</p><p><strong>-v, –verbose</strong><br>启用详细输出。</p><p>如果设置了VERBOSE环境变量，则可以省略此选项</p><p><strong>Run cmake –install with no options for quick help.</strong></p><h4 id="运行脚本"><a href="#运行脚本" class="headerlink" title="运行脚本"></a>运行脚本</h4><p><strong>cmake [-D &lt;var&gt;=&lt;value&gt;]… -P &lt;cmake-script-file&gt; [– &lt;unparsed-options&gt;…]</strong>  </p><p><strong>-D &lt;var&gt;=&lt;value&gt;</strong><br>定义脚本模式的变量。</p><p><strong>-P &lt;cmake-script-file&gt;</strong><br>将给定的cmake文件作为用cmake语言编写的脚本处理。不执行配置或生成步骤，也不修改缓存。如果使用-D定义变量，则必须在-P参数之前执行此操作</p>]]></content>
      
      
      <categories>
          
          <category> 编译安装 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>cmake基本使用（构建系统）</title>
      <link href="/2022/12/16/cmakedesc1/"/>
      <url>/2022/12/16/cmakedesc1/</url>
      
        <content type="html"><![CDATA[<h1 id="cmake基本使用（构建系统）"><a href="#cmake基本使用（构建系统）" class="headerlink" title="cmake基本使用（构建系统）"></a>cmake基本使用（构建系统）</h1><p>参考：<br>官方文档和官方教程：<br><a href="https://cmake.org/cmake/help/latest/index.html">https://cmake.org/cmake/help/latest/index.html</a><br><a href="https://cmake.org/cmake/help/latest/guide/tutorial/index.html">https://cmake.org/cmake/help/latest/guide/tutorial/index.html</a></p><h2 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h2><p>1.先自己跟着教程走一遍，有个大概了解  </p><p><img src="/images/cmakedesccomm1.jpg"></p><p>2.命令行–这里只讲解cmake命令，ctest和cpack都比较简单，自己去官网上看看就好<br><img src="/images/cmakedesccomm2.jpg"></p><p>3.其他，比如构建系统、cmake里面的命令、cmake内部属性变量、cmake生成器表达式、cmake语法等等<br>只讲如下标红框的内容</p><p><img src="/images/cmakedesccomm3.jpg"></p><h2 id="cmake构建系统"><a href="#cmake构建系统" class="headerlink" title="cmake构建系统"></a>cmake构建系统</h2><h3 id="二进制目标"><a href="#二进制目标" class="headerlink" title="二进制目标"></a>二进制目标</h3><p>可执行文件和库是使用add_executable()和add_library()命令定义的。生成的二进制文件具有针对目标平台的适当前缀、后缀和扩展名。二进制目标之间的依赖关系使用target_link_libraries()命令表示</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive archive.cpp zip.cpp lzma.cpp<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>zipapp zipapp.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>zipapp archive<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>archive被定义为一个STATIC库——一个包含从archive.cpp、zip.cpp和lzma.cpp编译的对象的存档。Zipapp被定义为通过编译和链接Zipapp .cpp形成的可执行文件。当链接zipapp可执行文件时，会链接到archive静态库</p><h3 id="二进制可执行文件"><a href="#二进制可执行文件" class="headerlink" title="二进制可执行文件"></a>二进制可执行文件</h3><p>add_executable()命令定义了一个可执行目标:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_executable</span> <span class="token punctuation">(</span>mytool mytool.cpp）<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>像add_custom_command()这样生成在构建时运行的规则的命令，可以透明地使用可执行目标作为COMMAND可执行文件。buildsystem规则将确保在尝试运行命令之前构建可执行文件。</p><h3 id="二进制库类型"><a href="#二进制库类型" class="headerlink" title="二进制库类型"></a>二进制库类型</h3><h4 id="普通类型"><a href="#普通类型" class="headerlink" title="普通类型"></a>普通类型</h4><p>默认情况下，add_library()命令定义一个STATIC库，除非指定了类型。使用命令时可以指定类型:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive <span class="token namespace">SHARED</span> archive.cpp zip.cpp lzma.cpp<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive <span class="token namespace">STATIC</span> archive.cpp zip.cpp lzma.cpp<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>在默认情况下，可以启用BUILD_SHARED_LIBS变量来更改add_library()构建共享库的行为。</p><p>在构建系统定义的上下文中，特定的库是SHARED还是STATIC在很大程度上无关紧要——不管库类型如何，命令、依赖规范和其他api都类似地工作。MODULE库类型是不同的，因为它通常不被链接到——它不在target_link_libraries()命令的右边使用。它是一种使用运行时技术作为插件加载的类型。如果库不导出任何非托管符号(例如Windows资源DLL, c++ /CLI DLL)，则要求该库不是SHARED库，因为CMake期望SHARED库至少导出一个符号。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive MODULE 7z.cpp<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h4 id="对象库"><a href="#对象库" class="headerlink" title="对象库"></a>对象库</h4><p>OBJECT库类型定义了编译给定源文件产生的目标文件的非归档集合。目标文件集合可以使用$ 语法作为其他目标的源输入。这是一个生成器表达式，可用于向其他目标提供OBJECT库内容:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive OBJECT archive.cpp zip.cpp lzma.cpp<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>archiveExtras <span class="token namespace">STATIC</span> <span class="token punctuation">$&lt;</span><span class="token namespace">TARGET_OBJECTS</span>:archive<span class="token punctuation">></span> extras.cpp<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>test_exe <span class="token punctuation">$&lt;</span><span class="token namespace">TARGET_OBJECTS</span>:archive<span class="token punctuation">></span> test.cpp<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这些其他目标的链接(或归档)步骤除了使用来自它们自己源的文件集合外，还将使用目标文件集合。</p><p>或者，对象库可以链接到其他目标:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive OBJECT archive.cpp zip.cpp lzma.cpp<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>archiveExtras <span class="token namespace">STATIC</span> extras.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>archiveExtras <span class="token namespace">PUBLIC</span> archive<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>test_exe test.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>test_exe archive<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这些其他目标的链接(或存档)步骤将使用直接链接的object库中的目标文件。此外，当在其他目标中编译源代码时，OBJECT库的使用需求将得到满足。此外，这些使用需求将传递到那些其他目标的依赖项。</p><p>在使用add_custom_command(TARGET)命令签名时，对象库不能用作TARGET。但是，对象列表可以通过add_custom_command(OUTPUT)或file(GENERATE)使用$ 来使用</p><h3 id="构建规范和使用需求"><a href="#构建规范和使用需求" class="headerlink" title="构建规范和使用需求"></a>构建规范和使用需求</h3><p>target_include_directories()、target_compile_definitions()和target_compile_options()命令指定构建规范和二进制目标的使用要求。这些命令分别填充INCLUDE_DIRECTORIES、COMPILE_DEFINITIONS和COMPILE_OPTIONS目标属性，和/或INTERFACE_INCLUDE_DIRECTORIES、INTERFACE_COMPILE_DEFINITIONS和INTERFACE_COMPILE_OPTIONS目标属性</p><p>每个命令都有PRIVATE、PUBLIC和INTERFACE模式。PRIVATE模式只填充目标属性的非INTERFACE_变量，而INTERFACE模式只填充INTERFACE_变量。PUBLIC模式填充各自目标属性的两个变体。每个命令可以通过多个关键字调用:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>archive  <span class="token namespace">PRIVATE</span> BUILDING_WITH_LZMA  <span class="token namespace">INTERFACE</span> USING_ARCHIVE_LIB<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h3 id="目标属性"><a href="#目标属性" class="headerlink" title="目标属性"></a>目标属性</h3><p>在编译二进制目标的源文件时，适当地使用INCLUDE_DIRECTORIES、COMPILE_DEFINITIONS和COMPILE_OPTIONS目标属性的内容。</p><p>INCLUDE_DIRECTORIES中的条目按照属性值中出现的顺序，使用-I或-isystem前缀添加到编译行。</p><p>COMPILE_DEFINITIONS中的条目以-D或/D作为前缀，并以未指定的顺序添加到编译行。DEFINE_SYMBOL目标属性也作为编译定义添加，作为SHARED和MODULE库目标的特殊方便情况。</p><p>COMPILE_OPTIONS中的条目将对shell进行转义，并按照属性值中出现的顺序添加。一些编译选项具有特殊的单独处理，例如POSITION_INDEPENDENT_CODE。</p><p>INTERFACE_INCLUDE_DIRECTORIES、INTERFACE_COMPILE_DEFINITIONS和INTERFACE_COMPILE_OPTIONS目标属性的内容是Usage Requirements——它们指定了使用者必须使用的内容，以便正确编译并链接到它们所出现的目标。对于任何二进制目标，target_link_libraries()命令中指定的每个目标上的每个INTERFACE_属性的内容将被消耗:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">set</span><span class="token punctuation">(</span>srcs archive.cpp zip.cpp<span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">(</span>LZMA_FOUND<span class="token punctuation">)</span>  <span class="token keyword">list</span><span class="token punctuation">(</span>APPEND srcs lzma.cpp<span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive <span class="token namespace">SHARED</span> <span class="token punctuation">$&#123;</span>srcs<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">(</span>LZMA_FOUND<span class="token punctuation">)</span>  <span class="token comment"># The archive library sources are compiled with -DBUILDING_WITH_LZMA</span>  <span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>archive <span class="token namespace">PRIVATE</span> BUILDING_WITH_LZMA<span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>archive <span class="token namespace">INTERFACE</span> USING_ARCHIVE_LIB<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>consumer<span class="token punctuation">)</span><span class="token comment"># Link consumer to archive and consume its usage requirements. The consumer</span><span class="token comment"># executable sources are compiled with -DUSING_ARCHIVE_LIB.</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>consumer archive<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>因为通常需要将源目录和相应的构建目录添加到INCLUDE_DIRECTORIES中，所以可以启用CMAKE_INCLUDE_CURRENT_DIR变量，以便方便地将相应的目录添加到所有目标的INCLUDE_DIRECTORIES中。可以启用变量CMAKE_INCLUDE_CURRENT_DIR_IN_INTERFACE，将相应的目录添加到所有目标的INTERFACE_INCLUDE_DIRECTORIES中。通过使用target_link_libraries()命令，可以方便地使用多个不同目录中的目标器。</p><h3 id="传递使用要求"><a href="#传递使用要求" class="headerlink" title="传递使用要求"></a>传递使用要求</h3><p>目标的使用需求可以传递到依赖项。target_link_libraries()命令具有PRIVATE、INTERFACE和PUBLIC关键字来控制传播。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>archive archive.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>archive <span class="token namespace">INTERFACE</span> USING_ARCHIVE_LIB<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>serialization serialization.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>serialization <span class="token namespace">INTERFACE</span> USING_SERIALIZATION_LIB<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>archiveExtras extras.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>archiveExtras <span class="token namespace">PUBLIC</span> archive<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>archiveExtras <span class="token namespace">PRIVATE</span> serialization<span class="token punctuation">)</span><span class="token comment"># archiveExtras is compiled with -DUSING_ARCHIVE_LIB</span><span class="token comment"># and -DUSING_SERIALIZATION_LIB</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>consumer consumer.cpp<span class="token punctuation">)</span><span class="token comment"># consumer is compiled with -DUSING_ARCHIVE_LIB</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>consumer archiveExtras<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>因为存档是archiveExtras的PUBLIC依赖项，所以它的使用需求也会传播给使用者。</p><p>因为序列化是archiveExtras的PRIVATE依赖项，所以它的使用需求不会传播给使用者。</p><p>通常，如果依赖项仅由库的实现使用，则应该在target_link_libraries()的使用中用PRIVATE关键字指定依赖项，而不是在头文件中指定。如果一个依赖项在库的头文件中被额外使用(例如用于类继承)，那么它应该被指定为PUBLIC依赖项。一个不被库的实现使用，而只被它的头使用的依赖项应该被指定为INTERFACE依赖项。target_link_libraries()命令可以通过多次使用每个关键字来调用:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>archiveExtras  <span class="token namespace">PUBLIC</span> archive  <span class="token namespace">PRIVATE</span> serialization<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>通过从依赖项中读取目标属性的INTERFACE_变量，并将值附加到操作数的非INTERFACE_变量，来传播使用需求。例如，读取依赖项的INTERFACE_INCLUDE_DIRECTORIES并将其追加到操作数的INCLUDE_DIRECTORIES中。如果顺序是相关的并得到了维护，并且target_link_libraries()调用产生的顺序不允许正确编译，则使用适当的命令直接设置属性可能会更新顺序。</p><p>例如，如果一个目标的链接库必须按照lib1 lib2 lib3的顺序指定，但是包含目录必须按照lib3 lib1 lib2的顺序指定:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myExe lib1 lib2 lib3<span class="token punctuation">)</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>myExe  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span>TARGET_PROPERTY:lib3,<span class="token property">INTERFACE_INCLUDE_DIRECTORIES</span><span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h3 id="兼容接口属性"><a href="#兼容接口属性" class="headerlink" title="兼容接口属性"></a>兼容接口属性</h3><p>某些目标属性需要在目标和每个依赖项的接口之间兼容。例如，POSITION_INDEPENDENT_CODE目标属性可以指定一个布尔值，表示是否应该将目标编译为与位置无关的代码，这将产生特定于平台的结果。目标还可以指定使用需求INTERFACE_POSITION_INDEPENDENT_CODE，以传达消费者必须被编译为位置无关代码。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET exe1 PROPERTY <span class="token property">POSITION_INDEPENDENT_CODE</span> <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 <span class="token namespace">SHARED</span> lib1.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1 PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe2 exe2.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe2 lib1<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这里，exe1和exe2都将被编译为位置无关的代码。lib1也将被编译为位置无关代码，因为这是SHARED库的默认设置。如果依赖关系存在冲突、不兼容的需求，cmake(1)将发出诊断:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 <span class="token namespace">SHARED</span> lib1.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1 PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 <span class="token namespace">SHARED</span> lib2.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib2 PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">OFF</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET exe1 PROPERTY <span class="token property">POSITION_INDEPENDENT_CODE</span> <span class="token boolean">OFF</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe2 exe2.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe2 lib1 lib2<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>lib1需求INTERFACE_POSITION_INDEPENDENT_CODE与exe1目标的POSITION_INDEPENDENT_CODE属性不“兼容”。标准库要求将消费者构建为与位置无关的代码，而可执行文件则指定不构建为与位置无关的代码，因此会发出诊断。</p><p>lib1和lib2的要求不“兼容”。其中一个要求将消费者构建为与位置无关的代码，而另一个要求将消费者构建为与位置无关的代码。因为ex2链接到两者，并且两者之间存在冲突，所以会发出一个CMake错误消息:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake">CMake Error: The <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> property of <span class="token string">"lib2"</span> doesnot agree with the value of <span class="token property">POSITION_INDEPENDENT_CODE</span> already determinedfor <span class="token string">"exe2"</span>.<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>为了“兼容”，POSITION_INDEPENDENT_CODE属性(如果设置了)必须在布尔意义上与INTERFACE_POSITION_INDEPENDENT_CODE属性相同，该属性所设置的所有传递指定依赖项的INTERFACE_POSITION_INDEPENDENT_CODE属性相同。</p><p>通过在COMPATIBLE_INTERFACE_BOOL目标属性的内容中指定该属性，“兼容接口要求”的属性可以扩展到其他属性。每个指定的属性必须在消费目标和对应的属性之间兼容，每个依赖项都有一个INTERFACE_前缀:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version2 <span class="token namespace">SHARED</span> lib1_v2.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 PROPERTY INTERFACE_CUSTOM_PROP <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 APPEND PROPERTY  <span class="token property">COMPATIBLE_INTERFACE_BOOL</span> CUSTOM_PROP<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version3 <span class="token namespace">SHARED</span> lib1_v3.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version3 PROPERTY INTERFACE_CUSTOM_PROP <span class="token boolean">OFF</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1Version2<span class="token punctuation">)</span> <span class="token comment"># CUSTOM_PROP will be ON</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe2 exe2.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe2 lib1Version2 lib1Version3<span class="token punctuation">)</span> <span class="token comment"># Diagnostic</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>非布尔属性也可以参与“兼容接口”计算。在COMPATIBLE_INTERFACE_STRING属性中指定的属性必须是未指定的，或者在所有传递指定的依赖项中与相同的字符串进行比较。这对于确保一个库的多个不兼容版本不会通过目标的传递需求链接在一起非常有用:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version2 <span class="token namespace">SHARED</span> lib1_v2.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 PROPERTY <span class="token variable">INTERFACE_LIB_VERSION</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 APPEND PROPERTY  <span class="token property">COMPATIBLE_INTERFACE_STRING</span> <span class="token variable">LIB_VERSION</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version3 <span class="token namespace">SHARED</span> lib1_v3.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version3 PROPERTY <span class="token variable">INTERFACE_LIB_VERSION</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1Version2<span class="token punctuation">)</span> <span class="token comment"># LIB_VERSION will be "2"</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe2 exe2.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe2 lib1Version2 lib1Version3<span class="token punctuation">)</span> <span class="token comment"># Diagnostic</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>COMPATIBLE_INTERFACE_NUMBER_MAX目标属性指定内容将以数字形式计算，并计算所有指定内容中的最大数量:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version2 <span class="token namespace">SHARED</span> lib1_v2.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 PROPERTY INTERFACE_CONTAINER_SIZE_REQUIRED <span class="token number">200</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 APPEND PROPERTY  <span class="token property">COMPATIBLE_INTERFACE_NUMBER_MAX</span> CONTAINER_SIZE_REQUIRED<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version3 <span class="token namespace">SHARED</span> lib1_v3.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version3 PROPERTY INTERFACE_CONTAINER_SIZE_REQUIRED <span class="token number">1000</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token comment"># CONTAINER_SIZE_REQUIRED will be "200"</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1Version2<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe2 exe2.cpp<span class="token punctuation">)</span><span class="token comment"># CONTAINER_SIZE_REQUIRED will be "1000"</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe2 lib1Version2 lib1Version3<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>类似地，COMPATIBLE_INTERFACE_NUMBER_MIN可用于从依赖项中计算属性的最小数值。</p><p>每个计算出的“compatible”属性值都可以在生成时使用生成器表达式在消费者中读取。</p><p>注意，对于每个依赖项，在每个兼容接口属性中指定的属性集不能与在任何其他属性中指定的属性集交叉</p><h3 id="属性来源调试"><a href="#属性来源调试" class="headerlink" title="属性来源调试"></a>属性来源调试</h3><p>因为构建规范可以通过依赖关系来确定，缺少创建目标的代码和负责设置构建规范的代码的局部性可能会使代码更加难以推理。Cmake(1)提供了一个调试工具来打印由依赖关系决定的属性内容的起源。可以调试的属性在CMAKE_DEBUG_TARGET_PROPERTIES变量文档中列出:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">set</span><span class="token punctuation">(</span><span class="token variable">CMAKE_DEBUG_TARGET_PROPERTIES</span>  <span class="token property">INCLUDE_DIRECTORIES</span>  <span class="token property">COMPILE_DEFINITIONS</span>  <span class="token property">POSITION_INDEPENDENT_CODE</span>  CONTAINER_SIZE_REQUIRED  <span class="token variable">LIB_VERSION</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>对于在COMPATIBLE_INTERFACE_BOOL或COMPATIBLE_INTERFACE_STRING中列出的属性，调试输出显示哪个目标负责设置该属性，以及哪些其他依赖项也定义了该属性。在COMPATIBLE_INTERFACE_NUMBER_MAX和COMPATIBLE_INTERFACE_NUMBER_MIN的情况下，调试输出显示每个依赖项的属性值，以及该值是否决定新的极值。</p><h3 id="使用生成器表达式构建规范"><a href="#使用生成器表达式构建规范" class="headerlink" title="使用生成器表达式构建规范"></a>使用生成器表达式构建规范</h3><p>构建规范可以使用生成器表达式，其中包含的内容可能是有条件的，或者只有在生成时才知道。例如，一个属性的计算“compatible”值可以通过TARGET_PROPERTY表达式读取:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1Version2 <span class="token namespace">SHARED</span> lib1_v2.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 PROPERTY  INTERFACE_CONTAINER_SIZE_REQUIRED <span class="token number">200</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib1Version2 APPEND PROPERTY  <span class="token property">COMPATIBLE_INTERFACE_NUMBER_MAX</span> CONTAINER_SIZE_REQUIRED<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1Version2<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>exe1 <span class="token namespace">PRIVATE</span>    CONTAINER_SIZE=<span class="token punctuation">$&lt;</span>TARGET_PROPERTY:CONTAINER_SIZE_REQUIRED<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在本例中，将使用-DCONTAINER_SIZE=200编译exe1源文件。</p><p>一元TARGET_PROPERTY生成器表达式和TARGET_POLICY生成器表达式用消费的目标上下文计算。这意味着使用需求规范可能会根据消费者的不同进行不同的评估:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 lib1.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>lib1 <span class="token namespace">INTERFACE</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">STREQUAL</span>:<span class="token punctuation">$&lt;</span>TARGET_PROPERTY:<span class="token property">TYPE</span><span class="token punctuation">></span>,EXECUTABLE<span class="token punctuation">></span>:LIB1_WITH_EXE<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">STREQUAL</span>:<span class="token punctuation">$&lt;</span>TARGET_PROPERTY:<span class="token property">TYPE</span><span class="token punctuation">></span>,SHARED_LIBRARY<span class="token punctuation">></span>:LIB1_WITH_SHARED_LIB<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>TARGET_POLICY:CMP0041<span class="token punctuation">></span>:CONSUMER_CMP0041_NEW<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1<span class="token punctuation">)</span><span class="token keyword">cmake_policy</span><span class="token punctuation">(</span>SET CMP0041 NEW<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>shared_lib shared_lib.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>shared_lib lib1<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>exe1可执行文件将使用-DLIB1_WITH_EXE编译，而shared_lib共享库将使用-DLIB1_WITH_SHARED_LIB和-DCONSUMER_CMP0041_NEW编译，因为在创建shared_lib目标时，策略CMP0041为NEW。</p><p>BUILD_INTERFACE表达式包装了一些需求，这些需求只在从同一个构建系统中的目标使用时使用，或者从使用export()命令导出到构建目录的目标使用时使用。INSTALL_INTERFACE表达式包装的需求仅在从已安装并使用install(EXPORT)命令导出的目标中使用:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>ClimbingStats climbingstats.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>ClimbingStats <span class="token namespace">INTERFACE</span>  <span class="token punctuation">$&lt;</span>BUILD_INTERFACE:ClimbingStats_FROM_BUILD_LOCATION<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span>INSTALL_INTERFACE:ClimbingStats_FROM_INSTALLED_LOCATION<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>TARGETS ClimbingStats EXPORT libExport <span class="token punctuation">$&#123;</span>InstallArgs<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>EXPORT libExport NAMESPACE Upstream::        DESTINATION lib/cmake/ClimbingStats<span class="token punctuation">)</span><span class="token keyword">export</span><span class="token punctuation">(</span>EXPORT libExport NAMESPACE Upstream::<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 ClimbingStats<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这种情况下，exe1可执行文件将使用-DClimbingStats_FROM_BUILD_LOCATION编译。导出命令生成的导入目标中省略了INSTALL_INTERFACE或BUILD_INTERFACE，并且去掉了*_INTERFACE标记。使用ClimbingStats包的单独项目将包含:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">find_package</span><span class="token punctuation">(</span>ClimbingStats REQUIRED<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>Downstream main.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>Downstream <span class="token inserted class-name">Upstream::ClimbingStats</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>根据ClimbingStats包是从构建位置还是安装位置使用，下游目标将使用-DClimbingStats_FROM_BUILD_LOCATION或-DClimbingStats_FROM_INSTALL_LOCATION编译。有关包和导出的更多信息，请参阅cmake-packages(7)手册。</p><h3 id="包括目录和使用要求"><a href="#包括目录和使用要求" class="headerlink" title="包括目录和使用要求"></a>包括目录和使用要求</h3><p>包含目录在作为使用需求指定时以及与生成器表达式一起使用时需要一些特殊的考虑。target_include_directories()命令同时接受相对和绝对包含目录:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 lib1.cpp<span class="token punctuation">)</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>lib1 <span class="token namespace">PRIVATE</span>  /absolute/path  relative/path<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>相对路径是相对于出现命令的源目录解释的。导入目标的INTERFACE_INCLUDE_DIRECTORIES中不允许存在相对路径。</p><p>在使用非平凡生成器表达式的情况下，可以在INSTALL_INTERFACE表达式的参数中使用INSTALL_PREFIX表达式。它是一个替换标记，在被消费项目导入时扩展为安装前缀。</p><p>包含目录的使用需求在构建树和安装树之间通常是不同的。BUILD_INTERFACE和INSTALL_INTERFACE生成器表达式可用于描述基于使用位置的单独使用需求。在INSTALL_INTERFACE表达式中允许使用相对路径，并相对于安装前缀进行解释。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>ClimbingStats climbingstats.cpp<span class="token punctuation">)</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>ClimbingStats <span class="token namespace">INTERFACE</span>  <span class="token punctuation">$&lt;</span>BUILD_INTERFACE:<span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_BINARY_DIR</span><span class="token punctuation">&#125;</span>/generated<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span>INSTALL_INTERFACE:/absolute/path<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span>INSTALL_INTERFACE:relative/path<span class="token punctuation">></span>  <span class="token punctuation">$&lt;</span>INSTALL_INTERFACE:<span class="token punctuation">$&lt;</span>INSTALL_PREFIX<span class="token punctuation">></span>/<span class="token punctuation">$&lt;</span>CONFIG<span class="token punctuation">></span>/generated<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>提供了两个与包含目录使用需求相关的方便api。CMAKE_INCLUDE_CURRENT_DIR_IN_INTERFACE变量可以被启用，其等效效果如下:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET tgt APPEND PROPERTY <span class="token property">INTERFACE_INCLUDE_DIRECTORIES</span>  <span class="token punctuation">$&lt;</span>BUILD_INTERFACE:<span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_SOURCE_DIR</span><span class="token punctuation">&#125;</span>;<span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_CURRENT_BINARY_DIR</span><span class="token punctuation">&#125;</span><span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>对于每个受影响的目标。安装目标的便利性是使用install(targets)命令的INCLUDES DESTINATION组件:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">install</span><span class="token punctuation">(</span>TARGETS foo bar bat EXPORT tgts <span class="token punctuation">$&#123;</span>dest_args<span class="token punctuation">&#125;</span>  INCLUDES DESTINATION include<span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>EXPORT tgts <span class="token punctuation">$&#123;</span>other_args<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>FILES <span class="token punctuation">$&#123;</span>headers<span class="token punctuation">&#125;</span> DESTINATION include<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这相当于在由install(EXPORT)生成时，将${CMAKE_INSTALL_PREFIX}/include附加到每个已安装导入目标的INTERFACE_INCLUDE_DIRECTORIES中。</p><p>当使用导入目标的INTERFACE_INCLUDE_DIRECTORIES时，属性中的条目被视为SYSTEM include目录，就好像它们列在依赖项的INTERFACE_SYSTEM_INCLUDE_DIRECTORIES中一样。这可能导致在这些目录中找到的头文件的编译器警告被忽略。导入目标的这种行为可以通过在导入目标的消费者上设置NO_SYSTEM_FROM_IMPORTED目标属性来控制，也可以通过在导入目标本身上设置IMPORTED_NO_SYSTEM目标属性来控制。</p><h3 id="链接库和生成器表达式"><a href="#链接库和生成器表达式" class="headerlink" title="链接库和生成器表达式"></a>链接库和生成器表达式</h3><p>与构建规范一样，可以使用生成器表达式条件指定链接库。然而，由于使用需求的消费是基于链接依赖项的收集，因此存在一个额外的限制，即链接依赖项必须形成一个“有向无循环图”。也就是说，如果对目标的链接依赖于目标属性的值，那么该目标属性可能不依赖于被链接的依赖项:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 lib1.cpp<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 lib2.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib1 <span class="token namespace">PUBLIC</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>TARGET_PROPERTY:<span class="token property">POSITION_INDEPENDENT_CODE</span><span class="token punctuation">></span>:lib2<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib3 lib3.cpp<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET lib3 PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 lib1 lib3<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>由于exe1目标的POSITION_INDEPENDENT_CODE属性的值依赖于被链接的库(lib3)，并且链接exe1的边缘是由相同的POSITION_INDEPENDENT_CODE属性决定的，因此上面的依赖关系图包含一个循环。Cmake(1)发出错误消息。</p><h3 id="输出构件"><a href="#输出构件" class="headerlink" title="输出构件"></a>输出构件</h3><p>由add_library()和add_executable()命令创建的buildsystem目标创建规则以创建二进制输出。二进制文件的准确输出位置只能在生成时确定，因为它可能取决于构建配置和链接依赖项的链接语言等。TARGET_FILE, TARGET_LINKER_FILE和相关的表达式可以用来访问生成的二进制文件的名称和位置。然而，这些表达式对OBJECT库不起作用，因为这些库生成的文件中没有一个与表达式相关的文件。</p><p>有三种类型的输出工件可以由目标构建，如下几节所述。它们的分类在DLL平台和非DLL平台之间有所不同。所有基于windows的系统包括Cygwin都是DLL平台。</p><h4 id="运行时输出工件"><a href="#运行时输出工件" class="headerlink" title="运行时输出工件"></a>运行时输出工件</h4><p>构建系统目标的运行时输出工件可能是:</p><p>由add_executable()命令创建的可执行目标的可执行文件(例如.exe)。</p><p>在DLL平台上:由add_library()命令使用shared选项创建的共享库目标的可执行文件(例如. DLL)。</p><p>RUNTIME_OUTPUT_DIRECTORY和RUNTIME_OUTPUT_NAME目标属性可以用来控制构建树中的运行时输出工件位置和名称。</p><h4 id="库输出工件"><a href="#库输出工件" class="headerlink" title="库输出工件"></a>库输出工件</h4><p>构建系统目标的库输出工件可能是:</p><p>由add_library()命令使用module选项创建的模块库目标的可加载模块文件(例如.dll或.so)。</p><p>在非dll平台上:由add_library()命令使用shared选项创建的共享库目标的共享库文件(例如。so或.dylib)。</p><p>LIBRARY_OUTPUT_DIRECTORY和LIBRARY_OUTPUT_NAME目标属性可用于控制构建树中的库输出工件位置和名称。</p><h4 id="存档输出工件"><a href="#存档输出工件" class="headerlink" title="存档输出工件"></a>存档输出工件</h4><p>构建系统目标的存档输出工件可能是:</p><p>由add_library()命令使用static选项创建的静态库目标的静态库文件(例如.lib或.a)。</p><p>在DLL平台上:由add_library()命令使用shared选项创建的共享库目标的导入库文件(例如.lib)。只有当库导出至少一个非托管符号时，此文件才保证存在。</p><p>在DLL平台上:当可执行目标的ENABLE_EXPORTS目标属性被设置时，由add_executable()命令创建的可执行目标的导入库文件(例如.lib)。</p><p>在AIX上:当设置了可执行目标的ENABLE_EXPORTS目标属性时，由add_executable()命令创建的可执行目标的链接器导入文件(例如.imp)。</p><p>ARCHIVE_OUTPUT_DIRECTORY和ARCHIVE_OUTPUT_NAME目标属性可用于控制生成树中的存档输出工件位置和名称。</p><h3 id="Directory-Scoped命令"><a href="#Directory-Scoped命令" class="headerlink" title="Directory-Scoped命令"></a>Directory-Scoped命令</h3><p>target_include_directories()， target_compile_definitions()和target_compile_options()命令一次只能对一个目标生效。命令add_compile_definitions()、add_compile_options()和include_directories()具有类似的函数，但是为了方便，在目录范围而不是目标范围内操作。</p><h3 id="构建配置"><a href="#构建配置" class="headerlink" title="构建配置"></a>构建配置</h3><p>配置确定特定类型构建的规范，例如发布或调试。指定的方式取决于所使用的生成器的类型。对于单个配置生成器(如Makefile generators和Ninja)，配置在配置时由CMAKE_BUILD_TYPE变量指定。对于多配置生成器，如Visual Studio, Xcode和Ninja Multi-Config，配置是由用户在构建时选择的，CMAKE_BUILD_TYPE被忽略。在多配置情况下，可用配置集在配置时由CMAKE_CONFIGURATION_TYPES变量指定，但实际使用的配置直到构建阶段才能知道。这种差异经常被误解，导致如下这样的有问题的代码:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># WARNING: This is wrong for multi-config generators because they don't use</span><span class="token comment">#          and typically don't even set CMAKE_BUILD_TYPE</span><span class="token keyword">string</span><span class="token punctuation">(</span>TOLOWER <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_BUILD_TYPE</span><span class="token punctuation">&#125;</span> build_type<span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">(</span>build_type <span class="token operator">STREQUAL</span> debug<span class="token punctuation">)</span>  <span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>exe1 <span class="token namespace">PRIVATE</span> DEBUG_BUILD<span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>应该使用生成器表达式来正确处理特定于配置的逻辑，而不管使用哪种生成器。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># Works correctly for both single and multi-config generators</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>exe1 <span class="token namespace">PRIVATE</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>CONFIG:Debug<span class="token punctuation">></span>:DEBUG_BUILD<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><p>在存在IMPORTED目标的情况下，MAP_IMPORTED_CONFIG_DEBUG的内容也由上面的$ 表达式负责</p><h4 id="区分大小写"><a href="#区分大小写" class="headerlink" title="区分大小写"></a>区分大小写</h4><p>CMAKE_BUILD_TYPE和CMAKE_CONFIGURATION_TYPES就像其他变量一样，与它们的值进行的任何字符串比较都是区分大小写的。$ 生成器表达式还保留用户或CMake默认设置的配置外壳。例如</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># NOTE: Don't use these patterns, they are for illustration purposes only.</span><span class="token keyword">set</span><span class="token punctuation">(</span><span class="token variable">CMAKE_BUILD_TYPE</span> Debug<span class="token punctuation">)</span><span class="token keyword">if</span><span class="token punctuation">(</span><span class="token variable">CMAKE_BUILD_TYPE</span> <span class="token operator">STREQUAL</span> DEBUG<span class="token punctuation">)</span>  <span class="token comment"># ... will never get here, "Debug" != "DEBUG"</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>print_config ALL  <span class="token comment"># Prints "Config is Debug" in this single-config case</span>  COMMAND <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_COMMAND</span><span class="token punctuation">&#125;</span> -E echo <span class="token string">"Config is $&lt;CONFIG>"</span>  VERBATIM<span class="token punctuation">)</span><span class="token keyword">set</span><span class="token punctuation">(</span><span class="token variable">CMAKE_CONFIGURATION_TYPES</span> Debug Release<span class="token punctuation">)</span><span class="token keyword">if</span><span class="token punctuation">(</span>DEBUG IN_LIST <span class="token variable">CMAKE_CONFIGURATION_TYPES</span><span class="token punctuation">)</span>  <span class="token comment"># ... will never get here, "Debug" != "DEBUG"</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>相反，CMake在内部根据配置修改行为的地方使用配置类型时，不区分大小写。例如，$ 生成器表达式对于不仅是Debug，而且是Debug、Debug甚至Debug的配置将计算为1。因此，你可以在CMAKE_BUILD_TYPE和CMAKE_CONFIGURATION_TYPES中指定配置类型，使用任何大小写的混合，尽管有严格的约定(参见下一节)。如果必须在字符串比较中测试值，总是先将值转换为大写或小写，并相应地调整测试。</p><h3 id="默认配置和自定义配置"><a href="#默认配置和自定义配置" class="headerlink" title="默认配置和自定义配置"></a>默认配置和自定义配置</h3><p>默认情况下，CMake定义了一些标准配置:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake">DebugReleaseRelWithDebInfoMinSizeRel<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在多配置生成器中，CMAKE_CONFIGURATION_TYPES变量将默认使用(可能是上述列表的子集)填充，除非被项目或用户覆盖。实际使用的配置由用户在构建时选择。</p><p>对于单配置生成器，配置是在配置时用CMAKE_BUILD_TYPE变量指定的，不能在构建时更改。默认值通常不是上述标准配置中的任何一个，而是一个空字符串。一个常见的误解是，这与Debug相同，但事实并非如此。用户应该始终显式地指定构建类型，以避免这个常见问题。</p><p>上述标准配置类型在大多数平台上提供了合理的行为，但它们可以扩展为提供其他类型。每个配置都为所使用的语言定义了一组编译器和链接器标志变量。这些变量遵循约定CMAKE_ <em>FLAGS</em> ，其中 始终是大写配置名。在定义自定义配置类型时，请确保适当地设置了这些变量，通常是作为缓存变量。</p><h3 id="伪目标"><a href="#伪目标" class="headerlink" title="伪目标"></a>伪目标</h3><p>一些目标类型并不表示构建系统的输出，而只是诸如外部依赖关系、别名或其他非构建构件等输入。伪目标不表示在生成的构建系统中。</p><h3 id="imported目标"><a href="#imported目标" class="headerlink" title="imported目标"></a>imported目标</h3><p>IMPORTED目标表示已存在的依赖项。通常这样的目标是由上游包定义的，应该被视为不可变的。在声明了一个IMPORTED目标之后，就可以像处理其他常规目标一样，使用常用的命令如target_compile_definitions()、target_include_directories()、target_compile_options()或target_link_libraries()来调整它的目标属性。</p><p>导入的目标可能具有与二进制目标相同的使用需求属性，例如INTERFACE_INCLUDE_DIRECTORIES、INTERFACE_COMPILE_DEFINITIONS、INTERFACE_COMPILE_OPTIONS、INTERFACE_LINK_LIBRARIES和INTERFACE_POSITION_INDEPENDENT_CODE。</p><p>LOCATION也可以从IMPORTED目标中读取，尽管很少有理由这样做。像add_custom_command()这样的命令可以透明地使用导入的可执行目标作为COMMAND可执行文件。</p><p>IMPORTED目标的定义范围是定义它的目录。它可以从子目录中访问和使用，但不能从父目录或兄弟目录中访问和使用。作用域类似于cmake变量的作用域。</p><p>也可以定义一个GLOBAL IMPORTED目标，它可以在构建系统中全局访问。</p><p>有关使用IMPORTED目标创建包的更多信息，请参阅cmake-packages(7)手册。</p><h3 id="别名的目标"><a href="#别名的目标" class="headerlink" title="别名的目标"></a>别名的目标</h3><p>ALIAS目标是一个在只读上下文中可以与二进制目标名称互换使用的名称。ALIAS目标的一个主要用例是附带库的单元测试可执行文件，这些库可以是同一个构建系统的一部分，也可以根据用户配置单独构建。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 lib1.cpp<span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>TARGETS lib1 EXPORT lib1Export <span class="token punctuation">$&#123;</span>dest_args<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>EXPORT lib1Export NAMESPACE Upstream:: <span class="token punctuation">$&#123;</span>other_args<span class="token punctuation">&#125;</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span><span class="token inserted class-name">Upstream::lib1</span> ALIAS lib1<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在另一个目录中，我们可以无条件地链接到Upstream::lib1目标，它可能是包中的IMPORTED目标，或者是作为同一个构建系统的一部分构建的ALIAS目标。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">NOT</span> TARGET <span class="token inserted class-name">Upstream::lib1</span><span class="token punctuation">)</span>  <span class="token keyword">find_package</span><span class="token punctuation">(</span>lib1 REQUIRED<span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 <span class="token inserted class-name">Upstream::lib1</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>ALIAS目标不可更改、不可安装或不可导出。它们完全是构建系统描述的局部。可以通过读取ALIASED_TARGET属性来测试一个名称是否为ALIAS名称:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">get_target_property</span><span class="token punctuation">(</span>_aliased <span class="token inserted class-name">Upstream::lib1</span> <span class="token property">ALIASED_TARGET</span><span class="token punctuation">)</span><span class="token keyword">if</span><span class="token punctuation">(</span>_aliased<span class="token punctuation">)</span>  <span class="token keyword">message</span><span class="token punctuation">(</span>STATUS <span class="token string">"The name Upstream::lib1 is an ALIAS for <span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">_aliased</span><span class="token punctuation">&#125;</span></span>."</span><span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h3 id="接口库"><a href="#接口库" class="headerlink" title="接口库"></a>接口库</h3><p>INTERFACE库目标不会编译源代码，也不会在磁盘上生成库工件，因此它没有LOCATION。</p><p>它可以指定使用要求，如INTERFACE_INCLUDE_DIRECTORIES, INTERFACE_COMPILE_DEFINITIONS, INTERFACE_COMPILE_OPTIONS, INTERFACE_LINK_LIBRARIES, INTERFACE_SOURCES和INTERFACE_POSITION_INDEPENDENT_CODE。只有target_include_directories()、target_compile_definitions()、target_compile_options()、target_sources()和target_link_libraries()命令的INTERFACE模式可以用于INTERFACE库。</p><p>从CMake 3.19开始，INTERFACE库目标可以有选择地包含源文件。包含源文件的接口库将作为构建目标包含在生成的构建系统中。它不编译源代码，但可能包含用于生成其他源代码的自定义命令。此外，ide将源文件显示为目标文件的一部分，以便进行交互式阅读和编辑。</p><p>INTERFACE库的一个主要用例是仅头文件的库。自CMake 3.23以来，头文件可以通过使用target_sources()命令将它们添加到头集来与库关联:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>Eigen <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">target_sources</span><span class="token punctuation">(</span>Eigen <span class="token namespace">INTERFACE</span>  FILE_SET HEADERS    BASE_DIRS src    FILES src/eigen.h src/vector.h src/matrix.h<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 Eigen<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当我们在这里指定FILE_SET时，我们定义的BASE_DIRS自动成为目标特征的使用需求中的包含目录。来自目标的使用需求在编译时被消耗和使用，但对链接没有影响。</p><p>另一个用例是为使用需求采用完全以目标为中心的设计:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>pic_on <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET pic_on PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">ON</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>pic_off <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET pic_off PROPERTY <span class="token property">INTERFACE_POSITION_INDEPENDENT_CODE</span> <span class="token boolean">OFF</span><span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>enable_rtti <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">target_compile_options</span><span class="token punctuation">(</span>enable_rtti <span class="token namespace">INTERFACE</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">OR</span>:<span class="token punctuation">$&lt;</span>COMPILER_ID:GNU<span class="token punctuation">></span>,<span class="token punctuation">$&lt;</span>COMPILER_ID:Clang<span class="token punctuation">></span><span class="token punctuation">></span>:-rtti<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe1 exe1.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe1 pic_on enable_rtti<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这样，exe1的构建规范完全表示为链接的目标，特定于编译器的标志的复杂性封装在一个INTERFACE库目标中。</p><p>可以安装和导出INTERFACE库。我们可以将默认头集与目标一起安装:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>Eigen <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">target_sources</span><span class="token punctuation">(</span>Eigen <span class="token namespace">INTERFACE</span>  FILE_SET HEADERS    BASE_DIRS src    FILES src/eigen.h src/vector.h src/matrix.h<span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>TARGETS Eigen EXPORT eigenExport  FILE_SET HEADERS DESTINATION include/Eigen<span class="token punctuation">)</span><span class="token keyword">install</span><span class="token punctuation">(</span>EXPORT eigenExport NAMESPACE Upstream::  DESTINATION lib/cmake/Eigen<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这里，头集中定义的头被安装以包括/Eigen。安装目的地自动变成一个包含目录，这是使用者的使用需求。</p>]]></content>
      
      
      <categories>
          
          <category> 编译安装 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>github仓库的创建、克隆、拉取和推送</title>
      <link href="/2022/12/09/githubrepo/"/>
      <url>/2022/12/09/githubrepo/</url>
      
        <content type="html"><![CDATA[<h2 id="github仓库的创建、克隆、拉取和推送"><a href="#github仓库的创建、克隆、拉取和推送" class="headerlink" title="github仓库的创建、克隆、拉取和推送"></a>github仓库的创建、克隆、拉取和推送</h2><p>本文主要讲解github仓库的创建、克隆到本地、拉取到本地、以及推送到github仓库</p><p>参考的文章：</p><p><a href="https://zhuanlan.zhihu.com/p/369486197">https://zhuanlan.zhihu.com/p/369486197</a><br><a href="https://blog.csdn.net/weixin_46622106/article/details/111914231">https://blog.csdn.net/weixin_46622106/article/details/111914231</a></p><h3 id="创建-GitHub-仓库的步骤及方法"><a href="#创建-GitHub-仓库的步骤及方法" class="headerlink" title="创建 GitHub 仓库的步骤及方法"></a>创建 GitHub 仓库的步骤及方法</h3><p><img src="/images/githubrepo1.webp"></p><p>如上图所示，此为博主的 GitHub 个人主页，点击Repositories，进入如下界面：</p><p>点击上图标注所示的绿色New按钮，进入下一步：</p><p><img src="/images/githubrepo2.webp"></p><p>标注 1：Repository name，仓库名称；</p><p>标注 2：Description，可选描述，也就是写不写都可以；</p><p>标注 3：Public，默认的仓库类型；</p><p>标注 4：Initialize this repository with a README，初始化仓库的信息文件，建议勾选。</p><p>如上图所示，这是创建 GitHub 仓库的核心页面，里面包含了众多信息。为了方便演示，博主已经把各种所需的信息都填写完啦！接下来，点击绿色Create repository按钮即可：</p><p><img src="/images/githubrepo3.webp"></p><p>如上图所示，我们已经把仓库创建成功啦！</p><p>仓库名为 CSBook ，包含 3 个commit，第一个 commit 是我们通过勾选Initialize this repository with a README，创建了一个初始化提交文件README.md，其中文件后缀为.md，表示文件为 Markdown 格式；包含 1 个branch，为master分支，即主分支；包含 1 个contributor，为贡献者，也就是我们自己</p><h3 id="GitHub-术语解释"><a href="#GitHub-术语解释" class="headerlink" title="GitHub 术语解释"></a>GitHub 术语解释</h3><p><strong>Repository</strong>：简称Repo，可以理解为“仓库”，我们的项目就存放在仓库之中。也就是说，如果我们想要建立项目，就得先建立仓库；有多个项目，就建立多个仓库。</p><p><strong>Issues</strong>：可以理解为“问题”，举一个简单的例子，如果我们开源一个项目，如果别人看了我们的项目，并且发现了bug，或者感觉那个地方有待改进，他就可以给我们提出Issue，等我们把Issues解决之后，就可以把这些Issues关闭；反之，我们也可以给他人提出Issue。</p><p><strong>Star</strong>：可以理解为“点赞”，当我们感觉某一个项目做的比较好之后，就可以为这个项目点赞，而且我们点赞过的项目，都会保存到我们的Star之中，方便我们随时查看。在 GitHub 之中，如果一个项目的点星数能够超百，那么说明这个项目已经很不错了。</p><p><strong>Fork</strong>：可以理解为“拉分支”，如果我们对某一个项目比较感兴趣，并且想在此基础之上开发新的功能，这时我们就可以Fork这个项目，这表示复制一个完成相同的项目到我们的 GitHub 账号之中，而且独立于原项目。之后，我们就可以在自己复制的项目中进行开发了。</p><p><strong>Pull Request</strong>：可以理解为“提交请求”，此功能是建立在Fork之上的，如果我们Fork了一个项目，对其进行了修改，而且感觉修改的还不错，我们就可以对原项目的拥有者提出一个Pull请求，等其对我们的请求审核，并且通过审核之后，就可以把我们修改过的内容合并到原项目之中，这时我们就成了该项目的贡献者。</p><p><strong>Merge</strong>：可以理解为“合并”，如果别人Fork了我们的项目，对其进行了修改，并且提出了Pull请求，这时我们就可以对这个Pull请求进行审核。如果这个Pull请求的内容满足我们的要求，并且跟我们原有的项目没有冲突的话，就可以将其合并到我们的项目之中。当然，是否进行合并，由我们决定。</p><p><strong>Watch</strong>：可以理解为“观察”，如果我们Watch了一个项目，之后，如果这个项目有了任何更新，我们都会在第一时候收到该项目的更新通知。</p><p><strong>Gist</strong>：如果我们没有项目可以开源或者只是单纯的想分享一些代码片段的话，我们就可以选择Gist。不过说心里话，如果不翻墙的话，Gist并不好用。</p><h3 id="安装git"><a href="#安装git" class="headerlink" title="安装git"></a>安装git</h3><p>git工具的安装我已安装，安装后可以用git clone或git remote add origin … 或git fecth或git pull来拉取githbu上的仓库到本地，具体安装方法和git基本使用方法可参考本文开头给的参考地址</p><h3 id="利用-SSH-完成-Git-与-GitHub-的绑定"><a href="#利用-SSH-完成-Git-与-GitHub-的绑定" class="headerlink" title="利用 SSH 完成 Git 与 GitHub 的绑定"></a>利用 SSH 完成 Git 与 GitHub 的绑定</h3><p>现在，我们已经对 GitHub 有了一定的了解，包括创建仓库、拉分支，或者通过Clone or download克隆或者下载代码；我们也下载并安装了 Git，也了解了其常用的命令。</p><p>But，无论是 GitHub，还是 Git，我们都是单独或者说是独立操作的，并没有将两者绑定啊！也就是说，我们现在只能通过 GitHub 下载代码，并不能通过 Git 向 GitHub 提交代码。</p><p>因此，在本篇博文中，我们就一起完成 Git 和 GitHub 的绑定，体验通过 Git 向 GitHub 提交代码的能力。不过在这之前，我们需要先了解 SSh（安全外壳协议），因为在 GitHub 上，一般都是通过 SSH 来授权的，而且大多数 Git 服务器也会选择使用 SSH 公钥来进行授权，所以想要向 GitHub 提交代码，首先就得在 GitHub 上添加 SSH key配置。</p><h4 id="第-1-步：生成-SSH-key"><a href="#第-1-步：生成-SSH-key" class="headerlink" title="第 1 步：生成 SSH key"></a>第 1 步：生成 SSH key</h4><p>我们要想生成SSH key，首先就得先安装 SSH，对于 Linux 和 Mac 系统，其默认是安装 SSH 的，而对于 Windows 系统，其默认是不安装 SSH 的，不过由于我们安装了 Git Bash，其也应该自带了 SSH. 可以通过在 Git Bash 中输入ssh命令，查看本机是否安装 SSH：</p><p><img src="/images/githubrepo4.webp"></p><p>如上图所示，此结果表示我们已经安装 SSH 啦！接下来，输入ssh-keygen -t rsa命令，表示我们指定 RSA 算法生成密钥，然后敲三次回车键，期间不需要输入密码，之后就就会生成两个文件，分别为id_rsa和id_rsa.pub，即密钥id_rsa和公钥id_rsa.pub. 对于这两个文件，其都为隐藏文件，默认生成在以下目录：</p><p>Linux 系统：~/.ssh</p><p>Mac 系统：~/.ssh</p><p>Windows 系统：C:\Documents and Settings\username\.ssh</p><p>Windows 10 ThinkPad：C:\Users\think.ssh</p><p>密钥和公钥生成之后，我们要做的事情就是把公钥id_rsa.pub的内容添加到 GitHub，这样我们本地的密钥id_rsa和 GitHub 上的公钥id_rsa.pub才可以进行匹配，授权成功后，就可以向 GitHub 提交代码啦！</p><h4 id="第-2-步：添加-SSH-key"><a href="#第-2-步：添加-SSH-key" class="headerlink" title="第 2 步：添加 SSH key"></a>第 2 步：添加 SSH key</h4><p><img src="/images/githubrepo5.webp"></p><p>如上图所示，进入我们的 GitHub 主页，先点击右上角所示的倒三角▽图标，然后再点击Settins，进行设置页面；点击我们的头像亦可直接进入设置页面：</p><p><img src="/images/githubrepo6.webp"></p><p>如上图所示，进入Settings页面后，再点击SSH and GPG Keys进入此子界面，然后点击New SSH key按钮：</p><p><img src="/images/githubrepo7.webp"></p><p>如上图所示，我们只需要将公钥id_rsa.pub的内容粘贴到Key处的位置（Titles的内容不填写也没事），然后点击Add SSH key 即可。</p><h4 id="第-3-步：验证绑定是否成功"><a href="#第-3-步：验证绑定是否成功" class="headerlink" title="第 3 步：验证绑定是否成功"></a>第 3 步：验证绑定是否成功</h4><p>在我们添加完SSH key之后，也没有明确的通知告诉我们绑定成功啊！不过我们可以通过在 Git Bash 中输入ssh -T <a href="mailto:&#x67;&#105;&#x74;&#64;&#103;&#x69;&#x74;&#x68;&#117;&#x62;&#46;&#99;&#111;&#x6d;">&#x67;&#105;&#x74;&#64;&#103;&#x69;&#x74;&#x68;&#117;&#x62;&#46;&#99;&#111;&#x6d;</a>进行测试：</p><p><img src="/images/githubrepo8.webp"></p><p>如上图所示，此结果即为Git 与 GitHub 绑定成功的标志</p><h3 id="通过-Git-将代码提交到-GitHub"><a href="#通过-Git-将代码提交到-GitHub" class="headerlink" title="通过 Git 将代码提交到 GitHub"></a>通过 Git 将代码提交到 GitHub</h3><p>当我们通过git clone或git remote add origin … 或git fecth或git pull来拉取githbu上的仓库到本地后，本地就和远程有了关联，我们可以通过命令 git push或git push origin master命令来推送本地代码到github仓库</p><p><img src="/images/githubrepo9.webp"></p><p>如上图所示，在第一次向远程仓库提交代码的时候，需要输入账号及密码进行验证，验证成功后，显示如下结果：</p><p><img src="/images/githubrepo10.webp"></p><p>然后，刷新 GitHub 中mybatis-tutorial仓库：</p><p><img src="/images/githubrepo20.webp"></p><p>如上图所示，我们已经将项目（仓库）中新添加的内容提交到了远程仓库。</p><h4 id="这里有个小问题"><a href="#这里有个小问题" class="headerlink" title="这里有个小问题"></a>这里有个小问题</h4><p>在首次使用git push 推送上传文件到远程仓库时总是跳出，说密码过着账号输入错误</p><p><img src="/images/githubrepo11.webp"></p><p>首次推送提示 输入的的2次用户名密码，第一次是输入github的登陆用户名和密码；<br>第二次是输入下面截图操作步骤最终填写的usernuserame（用户名） 和生成的tokens（密码）</p><p><strong>解决办法</strong>：<br>（首先登录GitHub网站登录你的git账号，做如下设置：</p><p>1.打开GitHub网站登录自己的gihub账号；<a href="https://github.com/">https://github.com</a></p><p>2.点击用户图小旁边的小三角，找到settings,打开它</p><p><img src="/images/githubrepo12.png"></p><p>3.找到Developer settings,找到Personal access tokens,找到Generate new token</p><p><img src="/images/githubrepo13.png"><br><img src="/images/githubrepo14.png"><br><img src="/images/githubrepo15.png"></p><p>4.填写第二次要输入的usernuserame，并勾选权限，生成token。选择要授予此令牌token的范围或权限。 要使用token从命令行访问仓库，请选择repo。 要使用token从命令行删除仓库，请选择delete_repo。 其他根据需要进行勾选。</p><p><img src="/images/githubrepo16.png"></p><p>5.最终填写的usernuserame（用户名） 和生成的tokens（密码）就是第二次需要填写用户名和密码 。</p><p><img src="/images/githubrepo17.png"></p><p>6.然后我们再次推送就成功了</p><p><img src="/images/githubrepo18.png"></p><p><img src="/images/githubrepo19.png"></p><p>7.有两种方式使用上面生成的token</p><ul><li>之后用自己生成的token登录，把上面生成的token粘贴到输入密码的位置</li></ul><blockquote><p>如果 push 等操作没有出现输入密码选项，请先输入如下命令，之后就可以看到输入密码选项了<br>git config –system –unset credential.helper</p></blockquote><ul><li>把token直接添加远程仓库链接中，这样就可以避免同一个仓库每次提交代码都要输入token了：</li></ul><blockquote><p>git remote set-url origin <a href="https://your_token@github.com/USERNAME/REPO.git">https://your_token@github.com/USERNAME/REPO.git</a><br>your_token：换成你自己得到的token<br>USERNAME：是你自己github的用户名<br>REPO：是你的仓库名称</p></blockquote><p><strong>附:</strong></p><h3 id="git-cloen-https和ssh的区别"><a href="#git-cloen-https和ssh的区别" class="headerlink" title="git cloen https和ssh的区别"></a>git cloen https和ssh的区别</h3><p>在git中clone项目有两种方式：HTTPS和SSH，它们的区别如下</p><p>HTTPS：不管是谁，拿到url随便clone，但是在push的时候需要验证用户名和密码；  </p><p>SSH：clone的项目你必须是拥有者或者管理员，而且需要在clone前添加SSH Key。SSH 在push的时候，是不需要输入用户名的，如果配置SSH key的时候设置了密码，则需要输入密码的，否则直接是不需要输入密码的</p><h3 id="GitHub的多人协作开发"><a href="#GitHub的多人协作开发" class="headerlink" title="GitHub的多人协作开发"></a>GitHub的多人协作开发</h3><p>一个项目可以由多个人进行访问开发，多人共同完成，然后推送到远程仓库中，除了项目所有者，其他人默认是没有推送权限的，需要项目所有者将其他开发人员添加进来并给予邀请</p><p>1、找到项目–&gt;Settings–&gt;Manage access（管理连接，看谁可以连接到这个项目仓库）–&gt;add</p><p><img src="/images/githubrepo21.jpg"></p><p>2、输入要邀请的开发人员的用户名</p><p><img src="/images/githubrepo22.png"></p><p>3、复制邀请地址，并将邀请地址给到合作的小伙伴：</p><p><img src="/images/githubrepo23.png"></p><p>4、小伙伴在其电脑打开地址：</p><p><img src="/images/githubrepo24.jpg"></p><p>5、小伙伴接收邀请（Accept Invitation）：</p><p><img src="/images/githubrepo25.jpg"></p><p>6、接下来就可以看到被授权的项目喇：</p><p><img src="/images/githubrepo26.jpg"></p><p>7、切换小伙伴的账号，将远程仓库的项目克隆到本地，然后测试推送修改到这个远程仓库</p><p>使用命令：git clone 项目仓库地址   来将远程仓库的项目克隆到本地</p><p><img src="/images/githubrepo27.png"></p><p>修改项目文件后进行推送：</p><p><img src="/images/githubrepo28.png"></p><p>注：1、由于使用的是克隆，所以会把源项目中的设置的远程地址别名也克隆过来</p><p>2、到了输入密码验证的地方还是输入当前用户的token口令</p><p><img src="/images/githubrepo29.png"></p><p>8、测试成功推送</p><p><img src="/images/githubrepo30.png"></p>]]></content>
      
      
      <categories>
          
          <category> 第三方技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> github </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>windows下mingw编译openssl</title>
      <link href="/2022/12/07/opensslcompile/"/>
      <url>/2022/12/07/opensslcompile/</url>
      
        <content type="html"><![CDATA[<h2 id="windows下用mingw编译openssl源码"><a href="#windows下用mingw编译openssl源码" class="headerlink" title="windows下用mingw编译openssl源码"></a>windows下用mingw编译openssl源码</h2><p>最近自己有写一个工具用第三方c++库请求一个https接口，由于直接请求http不让通过，所以只能请求https，但是请求https需要依赖openssl，第三方库我也是用mingw编译的，openssl我也打算用mingw编译，这样依赖上就没什么问题。</p><p>1.首先去openssl官网 <a href="https://www.openssl.org/">https://www.openssl.org/</a>，找到下载页面<a href="https://www.openssl.org/source/">https://www.openssl.org/source/</a>，拉到页面中部可以下载源代码，然后可以去往github托管的openssl，里面的说明要全面一些，<a href="https://github.com/openssl/openssl">https://github.com/openssl/openssl</a><br><img src="/images/opensslcompile1.jpg"><br><img src="/images/opensslcompile2.jpg"><br><img src="/images/opensslcompile3.jpg"></p><p>2.在github页面找到安装说明，然后找到window的安装说明，<a href="https://github.com/openssl/openssl/blob/master/NOTES-WINDOWS.md#native-builds-using-mingw">https://github.com/openssl/openssl/blob/master/NOTES-WINDOWS.md#native-builds-using-mingw</a><br>在这里面可以找到Native builds using MinGW，<br><img src="/images/opensslcompile4.jpg"></p><p>3.按照要求，并结合参考博客源写的文章<a href="https://blog.csdn.net/badayase/article/details/103921608">https://blog.csdn.net/badayase/article/details/103921608</a>，安装步骤如下：</p><ul><li>去往<a href="https://www.msys2.org/">https://www.msys2.org/</a>下载 <strong>mysys</strong></li><li>下载安装<strong>perl</strong>，目前mysys安装时已经自带perl,无需安装，如果你在mysys里面输入命令perl，找不到命令，那是需要安装的</li><li>由于我自己之前单独装了mingw，所以在mysys里面就不需要再安装make和gcc了，只需要让mysys能找到gcc和make即可，所以配置环境变量<strong>MSYS2_PATH_TYPE=inherit</strong>，msys2就会继承win系统的环境变量，去找寻win下已经配置好的mingw工具（需要重启shell窗口）。mysys命令行中输入gcc，没有提示找不到文件就说明成功了。</li><li>把下载好的源代码解压到mysys安装目录下的home目录下，比如我是解压在/home/openssl-1.1.1s下</li><li>进入mysys界面到/home/openssl-1.1.1s下执行<pre class="line-numbers language-none"><code class="language-none">.&#x2F;configure mingw64或.&#x2F;configure mingwmakemake install<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><img src="/images/opensslcompile5.jpg"></li></ul><p>4.我编译中遇到两个问题，一个是要在mysys安装目录下创建tmp目录，一个是需要以管理员身份运行mysys，编译没问题，但是安装是默认是安装到c盘program file的openssl目录下，需要管理员权限</p><p>至此就安装完毕了，安装后的内容如下：<br><img src="/images/opensslcompile6.jpg"><br><img src="/images/opensslcompile7.jpg"><br><img src="/images/opensslcompile8.jpg"></p>]]></content>
      
      
      <categories>
          
          <category> 编译安装 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>c++左值、右值、移动语义及完美转发</title>
      <link href="/2022/11/28/cppmove/"/>
      <url>/2022/11/28/cppmove/</url>
      
        <content type="html"><![CDATA[<h2 id="c-左值、右值、移动语义及完美转发"><a href="#c-左值、右值、移动语义及完美转发" class="headerlink" title="c++左值、右值、移动语义及完美转发"></a>c++左值、右值、移动语义及完美转发</h2><ul><li>左值、右值 </li></ul><p>概念1：</p><blockquote><p>左值：可以放到等号左边的东西叫左值。<br>右值：不可以放到等号左边的东西就叫右值。</p></blockquote><p>概念2：</p><blockquote><p>左值：可以取地址并且有名字的东西就是左值。<br>右值：不能取地址的没有名字的东西就是右值。</p></blockquote><p>左值一般有：</p><blockquote><p>函数名和变量名<br>返回左值引用的函数调用<br>前置自增自减表达式++i、–i<br>由赋值表达式或赋值运算符连接的表达式(a=b, a += b等)<br>解引用表达式*p<br>字符串字面值”abcd”  </p></blockquote><p>介绍右值前需要先介绍两个概念：纯右值和将亡值。</p><p>运算表达式产生的临时变量、不和对象关联的原始字面量、非引用返回的临时变量、lambda表达式等都是纯右值。<br>例如：</p><blockquote><p>除字符串字面值外的字面值<br>返回非引用类型的函数调用<br>后置自增自减表达式i++、i–<br>算术表达式(a+b, a*b, a&amp;&amp;b, a==b等)<br>取地址表达式等(&amp;a)</p></blockquote><p>而将亡值是指C++11新增的和右值引用相关的表达式，通常指将要被移动的对象、T&amp;&amp;函数的返回值、std::move函数的返回值、转换为T&amp;&amp;类型转换函数的返回值，将亡值可以理解为即将要销毁的值，通过“盗取”其它变量内存空间方式获取的值，在确保其它变量不再被使用或者即将被销毁时，可以避免内存空间的释放和分配，延长变量值的生命周期，常用来完成移动构造或者移动赋值的特殊任务</p><ul><li>左值引用、右值引用 </li></ul><p>根据名字大概就可以猜到意思，左值引用就是对左值进行引用的类型，右值引用就是对右值进行引用的类型，他们都是引用，都是对象的一个别名，并不拥有所绑定对象的内存，所以都必须立即初始化。<br>type &amp;name = exp; // 左值引用<br>type &amp;&amp;name = exp; // 右值引用</p><p>左值引用<br>看代码：</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">int</span> a <span class="token operator">=</span> <span class="token number">5</span><span class="token punctuation">;</span><span class="token keyword">int</span> <span class="token operator">&amp;</span>b <span class="token operator">=</span> a<span class="token punctuation">;</span> <span class="token comment">// b是左值引用</span>b <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span><span class="token keyword">int</span> <span class="token operator">&amp;</span>c <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span> <span class="token comment">// error，10无法取地址，无法进行引用</span><span class="token keyword">const</span> <span class="token keyword">int</span> <span class="token operator">&amp;</span>d <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span> <span class="token comment">// ok，因为是常引用，引用常量数字，这个常量数字会存储在内存中，可以取地址</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以得出结论：对于左值引用，等号右边的值必须可以取地址，如果不能取地址，则会编译失败，或者可以使用const引用形式，但这样就只能通过引用来读取输出，不能修改数组，因为是常量引用。</p><p>右值引用<br>如果使用右值引用，那表达式等号右边的值需要时右值，可以使用std::move函数强制把左值转换为右值。</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">int</span> a <span class="token operator">=</span> <span class="token number">4</span><span class="token punctuation">;</span><span class="token keyword">int</span> <span class="token operator">&amp;&amp;</span>b <span class="token operator">=</span> a<span class="token punctuation">;</span> <span class="token comment">// error, a是左值</span><span class="token keyword">int</span> <span class="token operator">&amp;&amp;</span>c <span class="token operator">=</span> std<span class="token double-colon punctuation">::</span><span class="token function">move</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// ok</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>移动语义  </li></ul><p>谈移动语义前，我们首先需要了解深拷贝与浅拷贝的概念</p><p>移动语义，在看来可以理解为转移所有权，之前的拷贝是对于别人的资源，自己重新分配一块内存存储复制过来的资源，而对于移动语义，类似于转让或者资源窃取的意思，对于那块资源，转为自己所拥有，别人不再拥有也不会再使用，通过C++11新增的移动语义可以省去很多拷贝负担，怎么利用移动语义呢，是通过移动构造函数</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">class</span> <span class="token class-name">A</span> <span class="token punctuation">&#123;</span><span class="token keyword">public</span><span class="token operator">:</span>    <span class="token function">A</span><span class="token punctuation">(</span><span class="token keyword">int</span> size<span class="token punctuation">)</span> <span class="token operator">:</span> <span class="token function">size_</span><span class="token punctuation">(</span>size<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>        data_ <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token keyword">int</span><span class="token punctuation">[</span>size<span class="token punctuation">]</span><span class="token punctuation">;</span>    <span class="token punctuation">&#125;</span>    <span class="token function">A</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">&#123;</span><span class="token punctuation">&#125;</span>    <span class="token function">A</span><span class="token punctuation">(</span><span class="token keyword">const</span> A<span class="token operator">&amp;</span> a<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>        size_ <span class="token operator">=</span> a<span class="token punctuation">.</span>size_<span class="token punctuation">;</span>        data_ <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token keyword">int</span><span class="token punctuation">[</span>size_<span class="token punctuation">]</span><span class="token punctuation">;</span>        cout <span class="token operator">&lt;&lt;</span> <span class="token string">"copy "</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>    <span class="token punctuation">&#125;</span>    <span class="token function">A</span><span class="token punctuation">(</span>A<span class="token operator">&amp;&amp;</span> a<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>        <span class="token keyword">this</span><span class="token operator">-></span>data_ <span class="token operator">=</span> a<span class="token punctuation">.</span>data_<span class="token punctuation">;</span>        a<span class="token punctuation">.</span>data_ <span class="token operator">=</span> <span class="token keyword">nullptr</span><span class="token punctuation">;</span>        cout <span class="token operator">&lt;&lt;</span> <span class="token string">"move "</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span>    <span class="token punctuation">&#125;</span>    <span class="token operator">~</span><span class="token function">A</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>        <span class="token keyword">if</span> <span class="token punctuation">(</span>data_ <span class="token operator">!=</span> <span class="token keyword">nullptr</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>         <span class="token keyword">delete</span><span class="token punctuation">[</span><span class="token punctuation">]</span> data_<span class="token punctuation">;</span>        <span class="token punctuation">&#125;</span>    <span class="token punctuation">&#125;</span>    <span class="token keyword">int</span> <span class="token operator">*</span>data_<span class="token punctuation">;</span>    <span class="token keyword">int</span> size_<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token punctuation">;</span><span class="token keyword">int</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>    A <span class="token function">a</span><span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    A b <span class="token operator">=</span> a<span class="token punctuation">;</span>    A c <span class="token operator">=</span> std<span class="token double-colon punctuation">::</span><span class="token function">move</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 调用移动构造函数</span>    <span class="token keyword">return</span> <span class="token number">0</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果不使用std::move()，会有很大的拷贝代价，使用移动语义可以避免很多无用的拷贝，提供程序性能，C++所有的STL都实现了移动语义，方便我们使用。例如：</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp">std<span class="token double-colon punctuation">::</span>vector<span class="token operator">&lt;</span>string<span class="token operator">></span> vecs<span class="token punctuation">;</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>std<span class="token double-colon punctuation">::</span>vector<span class="token operator">&lt;</span>string<span class="token operator">></span> vecm <span class="token operator">=</span> std<span class="token double-colon punctuation">::</span><span class="token function">move</span><span class="token punctuation">(</span>vecs<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 免去很多拷贝</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>完美转发  </li></ul><p>完美转发指可以写一个接受任意实参的函数模板，并转发到其它函数，目标函数会收到与转发函数完全相同的实参，转发函数实参是左值那目标函数实参也是左值，转发函数实参是右值那目标函数实参也是右值。那如何实现完美转发呢，答案是使用std::forward()。</p><pre class="line-numbers language-cpp" data-language="cpp"><code class="language-cpp"><span class="token keyword">void</span> <span class="token function">PrintV</span><span class="token punctuation">(</span><span class="token keyword">int</span> <span class="token operator">&amp;</span>t<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>cout <span class="token operator">&lt;&lt;</span> <span class="token string">"lvalue"</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token keyword">void</span> <span class="token function">PrintV</span><span class="token punctuation">(</span><span class="token keyword">int</span> <span class="token operator">&amp;&amp;</span>t<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>    cout <span class="token operator">&lt;&lt;</span> <span class="token string">"rvalue"</span> <span class="token operator">&lt;&lt;</span> endl<span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token keyword">template</span><span class="token operator">&lt;</span><span class="token keyword">typename</span> <span class="token class-name">T</span><span class="token operator">></span><span class="token keyword">void</span> <span class="token function">Test</span><span class="token punctuation">(</span>T <span class="token operator">&amp;&amp;</span>t<span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>    <span class="token function">PrintV</span><span class="token punctuation">(</span>t<span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token function">PrintV</span><span class="token punctuation">(</span>std<span class="token double-colon punctuation">::</span><span class="token generic-function"><span class="token function">forward</span><span class="token generic class-name"><span class="token operator">&lt;</span>T<span class="token operator">></span></span></span><span class="token punctuation">(</span>t<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>    <span class="token function">PrintV</span><span class="token punctuation">(</span>std<span class="token double-colon punctuation">::</span><span class="token function">move</span><span class="token punctuation">(</span>t<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span class="token keyword">int</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">&#123;</span>    <span class="token function">Test</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// lvalue rvalue rvalue</span>    <span class="token keyword">int</span> a <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>    <span class="token function">Test</span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// lvalue lvalue rvalue</span>    <span class="token function">Test</span><span class="token punctuation">(</span>std<span class="token double-colon punctuation">::</span><span class="token generic-function"><span class="token function">forward</span><span class="token generic class-name"><span class="token operator">&lt;</span><span class="token keyword">int</span><span class="token operator">></span></span></span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// lvalue rvalue rvalue</span>    <span class="token function">Test</span><span class="token punctuation">(</span>std<span class="token double-colon punctuation">::</span><span class="token generic-function"><span class="token function">forward</span><span class="token generic class-name"><span class="token operator">&lt;</span><span class="token keyword">int</span><span class="token operator">&amp;</span><span class="token operator">></span></span></span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// lvalue lvalue rvalue</span>    <span class="token function">Test</span><span class="token punctuation">(</span>std<span class="token double-colon punctuation">::</span><span class="token generic-function"><span class="token function">forward</span><span class="token generic class-name"><span class="token operator">&lt;</span><span class="token keyword">int</span><span class="token operator">&amp;&amp;</span><span class="token operator">></span></span></span><span class="token punctuation">(</span>a<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// lvalue rvalue rvalue</span>    <span class="token keyword">return</span> <span class="token number">0</span><span class="token punctuation">;</span><span class="token punctuation">&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>分析：</p><ul><li>Test(1)：1是右值，模板中T &amp;&amp;t这种为万能引用，右值1传到Test函数中变成了右值引用，但是调用PrintV()时候，t变成了左值，因为它变成了一个拥有名字的变量，所以打印lvalue，而PrintV(std::forward&lt;T&gt;(t))时候，会进行完美转发，按照原来的类型转发，所以打印rvalue，PrintV(std::move(t))毫无疑问会打印rvalue。</li><li>Test(a)：a是左值，模板中T &amp;&amp;这种为万能引用，左值a传到Test函数中变成了左值引用，所以有代码中打印。</li><li>Test(std::forward&lt;T&gt;(a))：转发为左值还是右值，依赖于T，T是左值那就转发为左值，T是右值那就转发为右值。</li></ul>]]></content>
      
      
      <categories>
          
          <category> 编程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>cmake生成器表达式</title>
      <link href="/2022/01/04/cmakedesc3/"/>
      <url>/2022/01/04/cmakedesc3/</url>
      
        <content type="html"><![CDATA[<h1 id="cmake生成器表达式"><a href="#cmake生成器表达式" class="headerlink" title="cmake生成器表达式"></a>cmake生成器表达式</h1><p>参考：<br>官方文档和官方教程：<br><a href="https://cmake.org/cmake/help/latest/index.html">https://cmake.org/cmake/help/latest/index.html</a><br><a href="https://cmake.org/cmake/help/latest/guide/tutorial/index.html">https://cmake.org/cmake/help/latest/guide/tutorial/index.html</a></p><h2 id="总览"><a href="#总览" class="headerlink" title="总览"></a>总览</h2><p>1.先自己跟着教程走一遍，有个大概了解  </p><p><img src="/images/cmakedesccomm1.jpg"></p><p>2.命令行–这里只讲解cmake命令，ctest和cpack都比较简单，自己去官网上看看就好<br><img src="/images/cmakedesccomm2.jpg"></p><p>3.其他，比如构建系统、cmake里面的命令、cmake内部属性变量、cmake生成器表达式、cmake语法等等<br>只讲如下标红框的内容</p><p><img src="/images/cmakedesccomm3.jpg"></p><h2 id="cmake生成器表达式-1"><a href="#cmake生成器表达式-1" class="headerlink" title="cmake生成器表达式"></a>cmake生成器表达式</h2><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>生成器表达式在生成构建系统期间进行计算，以生成特定于每个构建配置的信息。它们的形式是$&lt;…&gt;。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>tgt <span class="token namespace">PRIVATE</span> /opt/include/<span class="token punctuation">$&lt;</span>CXX_COMPILER_ID<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这将扩展到/opt/include/GNU， /opt/include/Clang等，这取决于所使用的c++编译器。</p><p>生成器表达式可以在许多目标属性的上下文中使用，如LINK_LIBRARIES、INCLUDE_DIRECTORIES、COMPILE_DEFINITIONS等。它们也可以在使用命令填充这些属性时使用，例如target_link_libraries()、target_include_directories()、target_compile_definitions()等。它们支持条件链接、编译时使用的条件定义、条件包含目录等等。条件可能基于构建配置、目标属性、平台信息或任何其他可查询信息。</p><p>生成器表达式可以嵌套:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>tgt <span class="token namespace">PRIVATE</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">VERSION_LESS</span>:<span class="token punctuation">$&lt;</span><span class="token variable">CXX_COMPILER_VERSION</span><span class="token punctuation">></span>,<span class="token number">4.2.0</span><span class="token punctuation">></span>:OLD_COMPILER<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果CMAKE_CXX_COMPILER_VERSION小于4.2.0，则上述内容将扩展为OLD_COMPILER。</p><h3 id="空格和引号"><a href="#空格和引号" class="headerlink" title="空格和引号"></a>空格和引号</h3><p>生成器表达式通常在命令参数之后进行解析。如果生成器表达式包含空格、新行、分号或其他可能被解释为命令参数分隔符的字符，则整个表达式在传递给命令时应该用引号括起来。如果不这样做，可能会导致表达式被拆分，并且可能不再将其识别为生成器表达式。</p><p>当使用add_custom_command()或add_custom_target()时，使用VERBATIM和COMMAND_EXPAND_LISTS选项来获得健壮的参数分割和引用。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># WRONG: Embedded space will be treated as an argument separator.</span><span class="token comment"># This ends up not being seen as a generator expression at all.</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>run_some_tool  COMMAND some_tool -I<span class="token punctuation">$&lt;</span>JOIN:<span class="token punctuation">$&lt;</span>TARGET_PROPERTY:tgt,<span class="token property">INCLUDE_DIRECTORIES</span><span class="token punctuation">></span>, -I<span class="token punctuation">></span>  VERBATIM<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># Better, but still not robust. Quotes prevent the space from splitting the</span><span class="token comment"># expression. However, the tool will receive the expanded value as a single</span><span class="token comment"># argument.</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>run_some_tool  COMMAND some_tool <span class="token string">"-I$&lt;JOIN:$&lt;TARGET_PROPERTY:tgt,INCLUDE_DIRECTORIES>, -I>"</span>  VERBATIM<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># Nearly correct. Using a semicolon to separate arguments and adding the</span><span class="token comment"># COMMAND_EXPAND_LISTS option means that paths with spaces will be handled</span><span class="token comment"># correctly. Quoting the whole expression ensures it is seen as a generator</span><span class="token comment"># expression. But if the target property is empty, we will get a bare -I</span><span class="token comment"># with nothing after it.</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>run_some_tool  COMMAND some_tool <span class="token string">"-I$&lt;JOIN:$&lt;TARGET_PROPERTY:tgt,INCLUDE_DIRECTORIES>,;-I>"</span>  COMMAND_EXPAND_LISTS  VERBATIM<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>使用变量构建更复杂的生成器表达式也是减少错误和提高可读性的好方法。上面的例子可以进一步改进如下:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># The $&lt;BOOL:...> check prevents adding anything if the property is empty,</span><span class="token comment"># assuming the property value cannot be one of CMake's false constants.</span><span class="token keyword">set</span><span class="token punctuation">(</span>prop <span class="token string">"$&lt;TARGET_PROPERTY:tgt,INCLUDE_DIRECTORIES>"</span><span class="token punctuation">)</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>run_some_tool  COMMAND some_tool <span class="token string">"$&lt;$&lt;BOOL:<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">prop</span><span class="token punctuation">&#125;</span></span>>:-I$&lt;JOIN:<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">prop</span><span class="token punctuation">&#125;</span></span>,;-I>>"</span>  COMMAND_EXPAND_LISTS  VERBATIM<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>一个常见的错误是尝试用缩进将生成器表达式分割到多行:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token comment"># WRONG: New lines and spaces all treated as argument separators, so the</span><span class="token comment"># generator expression is split and not recognized correctly.</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>tgt <span class="token namespace">PRIVATE</span>  <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">AND</span>:      <span class="token punctuation">$&lt;</span>CXX_COMPILER_ID:GNU<span class="token punctuation">></span>,      <span class="token punctuation">$&lt;</span>VERSION_GREATER_EQUAL:<span class="token punctuation">$&lt;</span><span class="token variable">CXX_COMPILER_VERSION</span><span class="token punctuation">></span>,<span class="token number">5</span><span class="token punctuation">></span>    <span class="token punctuation">></span>:HAVE_5_OR_LATER<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>同样，使用名称选择良好的辅助变量来构建可读的表达式:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">set</span><span class="token punctuation">(</span>is_gnu <span class="token string">"$&lt;CXX_COMPILER_ID:GNU>"</span><span class="token punctuation">)</span><span class="token keyword">set</span><span class="token punctuation">(</span>v5_or_later <span class="token string">"$&lt;VERSION_GREATER_EQUAL:$&lt;CXX_COMPILER_VERSION>,5>"</span><span class="token punctuation">)</span><span class="token keyword">set</span><span class="token punctuation">(</span>meet_requirements <span class="token string">"$&lt;AND:<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">is_gnu</span><span class="token punctuation">&#125;</span></span>,<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">v5_or_later</span><span class="token punctuation">&#125;</span></span>>"</span><span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>tgt <span class="token namespace">PRIVATE</span>  <span class="token string">"$&lt;<span class="token interpolation"><span class="token punctuation">$&#123;</span><span class="token variable">meet_requirements</span><span class="token punctuation">&#125;</span></span>:HAVE_5_OR_LATER>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="调试"><a href="#调试" class="headerlink" title="调试"></a>调试</h3><p>由于生成器表达式是在生成构建系统时计算的，而不是在处理CMakeLists.txt文件时计算的，因此不可能使用message()命令检查它们的结果。生成调试消息的一种可能的方法是添加一个自定义目标:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>genexdebug COMMAND <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_COMMAND</span><span class="token punctuation">&#125;</span> -E echo <span class="token string">"$&lt;...>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>运行cmake之后，您可以构建genexdebug目标以打印$&lt;…&gt;表达式(即执行命令cmake——build…——目标genexdebug)。</p><p>另一种方法是使用file(GENERATE)将调试消息写入文件:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">file</span><span class="token punctuation">(</span>GENERATE OUTPUT filename CONTENT <span class="token string">"$&lt;...>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="生成器表达式参考"><a href="#生成器表达式参考" class="headerlink" title="生成器表达式参考"></a>生成器表达式参考</h3><p>请注意 这个引用偏离了大多数CMake文档，因为它省略了尖括号&lt;…&gt;围绕占位符，如条件，字符串，目标等。这是为了防止那些占位符被错误地解释为生成器表达式。</p><h3 id="条件表达式"><a href="#条件表达式" class="headerlink" title="条件表达式"></a>条件表达式</h3><p>生成器表达式的一个基本类别与条件逻辑有关。支持两种形式的条件生成器表达式:</p><p>$&lt;condition:true_string&gt;<br>如果condition为1，则返回true_string;如果condition为0，则返回空字符串。condition的任何其他值都会导致错误。</p><p>$&lt;IF:condition,true_string,false_string&gt;<br>3.8新版功能。</p><p>如果condition为1，则返回true_string;如果condition为0，则返回false_string。condition的任何其他值都会导致错误。</p><p>通常，条件本身就是一个生成器表达式。例如，当使用Debug配置时，下面的表达式展开为DEBUG_MODE，对于所有其他配置则为空字符串:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>CONFIG:Debug<span class="token punctuation">></span>:DEBUG_MODE<span class="token punctuation">></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>除1或0之外的类似布尔的条件值可以用$<a href="BOOL:...">BOOL:...</a>生成器表达式:</p><p>$&lt;BOOL:string&gt;<br>将字符串转换为0或1。如果下列任意一个为真，则计算结果为0:</p><p>字符串为空，</p><p>string不区分大小写，等于0,FALSE, OFF, N, NO, IGNORE, NOTFOUND，或</p><p>字符串以后缀-NOTFOUND结尾(区分大小写)。</p><p>否则求值为1。</p><p>当CMake变量提供条件时，通常使用$&lt;BOOL:……&gt;生成器表达式:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>BOOL:<span class="token punctuation">$&#123;</span>HAVE_SOME_FEATURE<span class="token punctuation">&#125;</span><span class="token punctuation">></span>:-DENABLE_SOME_FEATURE<span class="token punctuation">></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="逻辑运算符"><a href="#逻辑运算符" class="headerlink" title="逻辑运算符"></a>逻辑运算符</h3><p>支持常见的布尔逻辑运算符:</p><p>$&lt;AND:conditions&gt;<br>其中conditions是一个以逗号分隔的布尔表达式列表，所有这些表达式的值必须为1或0。如果所有条件都为1，则整个表达式的值为1。如果任何条件为0，整个表达式的计算结果为0。</p><p>$&lt;OR:conditions&gt;<br>其中conditions是逗号分隔的布尔表达式列表。所有这些都必须等于1或0。如果至少有一个条件为1，则整个表达式的值为1。如果所有条件的值为0，则整个表达式的值为0。</p><p>$&lt;NOT:condition&gt;<br>Condition必须为0或1。如果condition为1，表达式的结果为0，否则为1。</p><h3 id="主要比较表达式"><a href="#主要比较表达式" class="headerlink" title="主要比较表达式"></a>主要比较表达式</h3><p>CMake支持各种生成器表达式进行比较。本节将介绍主要的和最广泛使用的比较类型。其他更具体的比较类型将在后面单独的部分中进行说明。</p><h3 id="字符串比较"><a href="#字符串比较" class="headerlink" title="字符串比较"></a>字符串比较</h3><p>$&lt;STREQUAL:string1,string2&gt;  </p><p>如果string1和string2相等，则为1，否则为0。比较是区分大小写的。要进行不区分大小写的比较，请与字符串转换生成器表达式结合使用。例如，如果${foo}是BAR, BAR, BAR等中的任意一个，则下面的计算结果为1。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token punctuation">$&lt;</span><span class="token operator">STREQUAL</span>:<span class="token punctuation">$&lt;</span>UPPER_CASE:<span class="token punctuation">$&#123;</span>foo<span class="token punctuation">&#125;</span><span class="token punctuation">></span>,BAR<span class="token punctuation">></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>$&lt;EQUAL:value1,value2&gt;<br>如果value1和value2数值相等，则为1，否则为0。</p><h3 id="版本比较"><a href="#版本比较" class="headerlink" title="版本比较"></a>版本比较</h3><p>$&lt;VERSION_LESS:v1,v2&gt;<br>如果v1小于v2，则为1，否则为0。</p><p>$&lt;VERSION_GREATER:v1,v2&gt;<br>如果v1的版本大于v2，则为1，否则为0。</p><p>$&lt;VERSION_EQUAL:v1,v2&gt;<br>如果v1和v2的版本相同，则为1，否则为0。</p><p>$&lt;VERSION_LESS_EQUAL:v1,v2&gt;<br>3.7新版功能。</p><p>如果v1小于或等于v2，则为1，否则为0。</p><p>$&lt;VERSION_GREATER_EQUAL:v1,v2&gt;<br>3.7新版功能。</p><p>如果v1是大于或等于v2的版本，则为1，否则为0。</p><h3 id="字符串转换"><a href="#字符串转换" class="headerlink" title="字符串转换"></a>字符串转换</h3><p>$&lt;LOWER_CASE:string&gt;<br>转换为小写的字符串内容。</p><p>$&lt;UPPER_CASE:string&gt;<br>转换为大写的字符串内容。</p><p>$&lt;MAKE_C_IDENTIFIER:…&gt;<br>的内容……转换为C标识符。转换遵循与string(MAKE_C_IDENTIFIER)相同的行为。</p><h3 id="表达式列表"><a href="#表达式列表" class="headerlink" title="表达式列表"></a>表达式列表</h3><p>$&lt;IN_LIST:string,list&gt;<br>3.12新版功能。</p><p>如果string是分号分隔列表中的项，则为1，否则为0。它使用区分大小写的比较。</p><p>$&lt;JOIN:list,string&gt;<br>用插入在每个项之间的字符串内容连接列表。</p><p>$&lt;REMOVE_DUPLICATES:list&gt;<br>3.15新版功能。</p><p>删除给定列表中的重复项。保留项的相对顺序，但如果遇到重复项，则只保留第一个实例。</p><p>$&lt;FILTER:list,INCLUDE|EXCLUDE,regex&gt;<br>3.15新版功能。</p><p>从列表中包含或删除与正则表达式regex匹配的项。</p><h3 id="路径表达式"><a href="#路径表达式" class="headerlink" title="路径表达式"></a>路径表达式</h3><p>本节中的大多数表达式都与cmake_path()命令密切相关，提供相同的功能，但是是以生成器表达式的形式。</p><p>对于本节中的所有生成器表达式，路径都应该是cmake样式的格式。$&lt;PATH:CMAKE_PATH&gt;生成器表达式可用于将本机路径转换为cmake样式的路径。</p><h3 id="路径的比较"><a href="#路径的比较" class="headerlink" title="路径的比较"></a>路径的比较</h3><p>$&lt;PATH_EQUAL:path1,path2&gt;<br>3.24新版功能。</p><p>比较两个路径的词法表示。在任何路径上都不执行归一化。如果路径相等则返回1，否则返回0。</p><p>有关更多细节，请参阅cmake_path(COMPARE)。</p><h3 id="路径查询"><a href="#路径查询" class="headerlink" title="路径查询"></a>路径查询</h3><p>这些表达式提供了等同于cmake_path()命令的Query选项的生成时功能。所有路径都应该是cmake样式的格式。</p><p>$&lt;PATH:HAS_*,path&gt;<br>3.24新版功能。</p><p>如果存在特定的路径组件，则返回1，否则返回0。有关每个路径组件的含义，请参阅路径结构和术语。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token punctuation">$&lt;</span>PATH:HAS_ROOT_NAME,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_ROOT_DIRECTORY,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_ROOT_PATH,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_FILENAME,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_EXTENSION,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_STEM,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_RELATIVE_PART,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:HAS_PARENT_PATH,path<span class="token punctuation">></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>注意以下特殊情况:</p><p>对于HAS_ROOT_PATH，只有当根名称或根目录中至少有一个非空时，才会返回true结果。</p><p>对于HAS_PARENT_PATH，根目录也被认为有一个父目录，即它本身。除非路径仅由文件名组成，否则结果为真。</p><p>$&lt;PATH:IS_ABSOLUTE,path&gt;<br>3.24新版功能。</p><p>如果路径是绝对路径则返回1，否则返回0。</p><p>$&lt;PATH:IS_RELATIVE,path&gt;<br>3.24新版功能。</p><p>这将返回与IS_ABSOLUTE相反的结果。</p><p>$&lt;PATH:IS_PREFIX[,NORMALIZE],path,input&gt;<br>3.24新版功能。</p><p>如果path是输入的前缀，则返回1，否则返回0。</p><p>当指定NORMALIZE选项时，路径和输入在检查之前被标准化。</p><h3 id="路径分解"><a href="#路径分解" class="headerlink" title="路径分解"></a>路径分解</h3><p>这些表达式提供了等同于cmake_path()命令的分解选项的生成时功能。所有路径都应该是cmake样式的格式。</p><p>$&lt;PATH:GET_*,…&gt;<br>3.24新版功能。</p><p>以下操作从路径中检索不同的组件或组件组。有关每个路径组件的含义，请参阅路径结构和术语。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token punctuation">$&lt;</span>PATH:GET_ROOT_NAME,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_ROOT_DIRECTORY,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_ROOT_PATH,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_FILENAME,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_EXTENSION[,LAST_ONLY],path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_STEM[,LAST_ONLY],path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_RELATIVE_PART,path<span class="token punctuation">></span><span class="token punctuation">$&lt;</span>PATH:GET_PARENT_PATH,path<span class="token punctuation">></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果请求的组件不在路径中，则返回空字符串。</p><h3 id="路径转换"><a href="#路径转换" class="headerlink" title="路径转换"></a>路径转换</h3><p>这些表达式提供了等同于cmake_path()命令的modify和Generation选项的生成时功能。所有路径都应该是cmake样式的格式。</p><p>$&lt;PATH:CMAKE_PATH[,NORMALIZE],path&gt;<br>3.24新版功能。</p><p>返回路径。如果path是原生路径，它将转换为带有正斜杠(/)的cmake样式的路径。在Windows上，长文件名标记会被考虑在内。</p><p>当指定NORMALIZE选项时，转换后将对路径进行规范化。</p><p>$&lt;PATH:APPEND,path,input,…&gt;<br>3.24新版功能。</p><p>返回以/作为目录分隔符附加到path的所有输入参数。根据输入的不同，path的值可能会被丢弃。</p><p>请参阅cmake_path(APPEND)了解更多详细信息。</p><p>$&lt;PATH:REMOVE_FILENAME,path&gt;<br>3.24新版功能。</p><p>返回删除了文件名组件(由$&lt; path:GET_FILENAME&gt;返回)的路径。删除之后，任何尾随的目录分隔符(如果存在的话)都将保持不变。</p><p>参见cmake_path(REMOVE_FILENAME)了解更多细节。</p><p>$&lt;PATH:REPLACE_FILENAME,path,input&gt;<br>3.24新版功能。</p><p>返回路径，其中filename组件被输入替换。如果path没有文件名组件(例如$&lt; path:HAS_FILENAME&gt;返回0)，path不变。</p><p>参见cmake_path(REPLACE_FILENAME)了解更多细节。</p><p>$&lt;PATH:REMOVE_EXTENSION[,LAST_ONLY],path&gt;<br>3.24新版功能。</p><p>返回已删除扩展名的路径(如果有)。</p><p>有关详细信息，请参阅cmake_path(REMOVE_EXTENSION)。</p><p>$&lt;PATH:REPLACE_EXTENSION[,LAST_ONLY],path,input&gt;<br>3.24新版功能。</p><p>返回路径，其中扩展名替换为输入(如果有的话)。</p><p>详细信息请参见cmake_path(REPLACE_EXTENSION)。</p><p>$&lt;PATH:NORMAL_PATH,path&gt;<br>3.24新版功能。</p><p>返回根据归一化中描述的步骤归一化的路径。</p><p>$&lt;PATH:RELATIVE_PATH,path,base_directory&gt;<br>3.24新版功能。</p><p>返回路径，修改后使其相对于base_directory参数。</p><p>有关更多细节，请参阅cmake_path(RELATIVE_PATH)。</p><p>$&lt;PATH:ABSOLUTE_PATH[,NORMALIZE],path,base_directory&gt;<br>3.24新版功能。</p><p>返回绝对路径。如果path是一个相对路径($&lt; path:IS_RELATIVE&gt;返回1)，它将相对于base_directory参数指定的给定基目录进行计算。</p><p>当指定NORMALIZE选项时，在路径计算之后对路径进行规范化。</p><p>有关详细信息，请参阅cmake_path(ABSOLUTE_PATH)。</p><h3 id="shell-path"><a href="#shell-path" class="headerlink" title="shell path"></a>shell path</h3><p>$&lt;SHELL_PATH:…&gt;<br>3.4新版功能。</p><p>的内容……转换为shell路径样式。例如，Windows shell中将斜杠转换为反斜杠，MSYS shell将盘符转换为posix路径。…必须为绝对路径。</p><p>3.14新版功能:The…可以是一个以分号分隔的路径列表，在这种情况下，每个路径都被单独转换，并且使用shell路径分隔符(:在POSIX和;在Windows上)。在CMake源代码中，请务必将包含此genex的参数括在双引号中，以便;不分裂争论。</p><h3 id="配置表达式"><a href="#配置表达式" class="headerlink" title="配置表达式"></a>配置表达式</h3><p>$&lt;CONFIG&gt;<br>配置名称。使用此表达式代替已弃用的CONFIGURATION生成器表达式。</p><p>$&lt;CONFIG:cfgs&gt;<br>如果config是逗号分隔的列表cfgs中的任何一项，则为1，否则为0。这是一个不区分大小写的比较。当在导入目标的属性上计算map_importted_config_ 中的映射时，此表达式也会考虑它。</p><p>在3.19版更改:可以为cfgs指定多个配置。CMake 3.18和更早的版本只接受单一配置。</p><p>$&lt;OUTPUT_CONFIG:…&gt;<br>3.20新版功能。</p><p>仅在add_custom_command()和add_custom_target()中作为参数中的最外层生成器表达式有效。与忍者多配置生成器，生成器表达式在…使用自定义命令的“输出配置”进行计算。使用其他生成器，…正常计算。</p><p>$&lt;COMMAND_CONFIG:…&gt;<br>3.20新版功能。</p><p>仅在add_custom_command()和add_custom_target()中作为参数中的最外层生成器表达式有效。与忍者多配置生成器，生成器表达式在…使用自定义命令的“命令配置”进行计算。使用其他生成器，…正常计算。</p><h3 id="工具链和语言表达"><a href="#工具链和语言表达" class="headerlink" title="工具链和语言表达"></a>工具链和语言表达</h3><h3 id="平台"><a href="#平台" class="headerlink" title="平台"></a>平台</h3><p>$<PLATFORM_ID><br>当前系统的CMake平台id。另请参阅CMAKE_SYSTEM_NAME变量。</p><p>$&lt;PLATFORM_ID:platform_ids&gt;<br>如果CMake的平台id与逗号分隔的platform_ids列表中的任何一个项匹配，则为1，否则为0。另请参阅CMAKE_SYSTEM_NAME变量。</p><h3 id="编译器版本"><a href="#编译器版本" class="headerlink" title="编译器版本"></a>编译器版本</h3><p>另请参阅CMAKE_ _COMPILER_VERSION变量，该变量与本小节中的表达式密切相关。</p><p>$<C_COMPILER_VERSION><br>使用的C编译器版本。</p><p>$&lt;C_COMPILER_VERSION:version&gt;<br>如果C编译器的版本与version匹配，则为1，否则为0。</p><p>$<CXX_COMPILER_VERSION><br>使用的CXX编译器的版本。</p><p>$&lt;CXX_COMPILER_VERSION:version&gt;<br>如果CXX编译器的版本与version匹配，则为1，否则为0。</p><p>$<CUDA_COMPILER_VERSION><br>3.15新版功能。</p><p>使用的CUDA编译器的版本。</p><p>$&lt;CUDA_COMPILER_VERSION:version&gt;<br>3.15新版功能。</p><p>如果CXX编译器的版本与version匹配，则为1，否则为0。</p><p>$<OBJC_COMPILER_VERSION><br>3.16新版功能。</p><p>使用的OBJC编译器的版本。</p><p>$&lt;OBJC_COMPILER_VERSION:version&gt;<br>3.16新版功能。</p><p>如果OBJC编译器的版本与version匹配，则为1，否则为0。</p><p>$<OBJCXX_COMPILER_VERSION><br>3.16新版功能。</p><p>使用的OBJCXX编译器的版本。</p><p>$&lt;OBJCXX_COMPILER_VERSION:version&gt;<br>3.16新版功能。</p><p>如果OBJCXX编译器的版本与version匹配，则返回1，否则返回0。</p><p>$<Fortran_COMPILER_VERSION><br>使用的Fortran编译器版本。</p><p>$&lt;Fortran_COMPILER_VERSION:version&gt;<br>如果Fortran编译器的版本与version匹配，则为1，否则为0。</p><p>$<HIP_COMPILER_VERSION><br>3.21新版功能。</p><p>使用的HIP编译器的版本。</p><p>$&lt;HIP_COMPILER_VERSION:version&gt;<br>3.21新版功能。</p><p>如果HIP编译器的版本与version匹配，则为1，否则为0。</p><p>$<ISPC_COMPILER_VERSION><br>3.19新版功能。</p><p>使用的ISPC编译器的版本。</p><p>$&lt;ISPC_COMPILER_VERSION:version&gt;<br>3.19新版功能。</p><p>如果ISPC编译器的版本与version匹配，则为1，否则为0。</p><h3 id="编译语言和ID"><a href="#编译语言和ID" class="headerlink" title="编译语言和ID"></a>编译语言和ID</h3><p>另请参阅CMAKE_ _COMPILER_ID变量，该变量与本小节中的大多数表达式密切相关。</p><p>$<C_COMPILER_ID><br>使用的C编译器的CMake的编译器id。</p><p>$&lt;C_COMPILER_ID:compiler_ids&gt;<br>其中compiler_ids是一个逗号分隔的列表。如果CMake的C编译器的编译器id与compiler_ids中的任何一个条目匹配，则返回1，否则为0。</p><p>$<CXX_COMPILER_ID><br>所使用的CXX编译器的CMake编译器id。</p><p>$&lt;CXX_COMPILER_ID:compiler_ids&gt;<br>其中compiler_ids是一个逗号分隔的列表。如果CXX编译器的CMake的编译器id与compiler_ids中的任何一个条目匹配，则返回1，否则为0。</p><p>$<CUDA_COMPILER_ID><br>3.15新版功能。</p><p>使用的CUDA编译器的CMake的编译器id。</p><p>$&lt;CUDA_COMPILER_ID:compiler_ids&gt;<br>3.15新版功能。</p><p>其中compiler_ids是一个逗号分隔的列表。如果CMake的CUDA编译器的编译器id与compiler_ids中的任何一个条目匹配，则为1，否则为0。</p><p>$<OBJC_COMPILER_ID><br>3.16新版功能。</p><p>使用的OBJC编译器的CMake的编译器id。</p><p>$&lt;OBJC_COMPILER_ID:compiler_ids&gt;<br>3.16新版功能。</p><p>其中compiler_ids是一个逗号分隔的列表。如果Objective-C编译器的CMake的编译器id与compiler_ids中的任何一个条目匹配，则返回1，否则为0。</p><p>$<OBJCXX_COMPILER_ID><br>3.16新版功能。</p><p>使用的OBJCXX编译器的CMake的编译器id。</p><p>$&lt;OBJCXX_COMPILER_ID:compiler_ids&gt;<br>3.16新版功能。</p><p>其中compiler_ids是一个逗号分隔的列表。如果CMake的objective - c++编译器的编译器id与compiler_ids中的任何一个条目匹配，则返回1，否则为0。</p><p>$<Fortran_COMPILER_ID><br>使用的Fortran编译器的CMake的编译器id。</p><p>$&lt;Fortran_COMPILER_ID:compiler_ids&gt;<br>其中compiler_ids是一个逗号分隔的列表。如果CMake的Fortran编译器的编译器id与compiler_ids中的任何一个条目匹配，则为1，否则为0。</p><p>$<HIP_COMPILER_ID><br>3.21新版功能。</p><p>使用的HIP编译器的CMake的编译器id。</p><p>$&lt;HIP_COMPILER_ID:compiler_ids&gt;<br>3.21新版功能。</p><p>其中compiler_ids是一个逗号分隔的列表。如果CMake的HIP编译器的编译器id与compiler_ids中的任何一个条目匹配，则返回1，否则为0。</p><p>$<ISPC_COMPILER_ID><br>3.19新版功能。</p><p>使用的ISPC编译器的CMake的编译器id。</p><p>$&lt;ISPC_COMPILER_ID:compiler_ids&gt;<br>3.19新版功能。</p><p>其中compiler_ids是一个逗号分隔的列表。如果CMake的ISPC编译器的编译器id与compiler_ids中的任何一个条目匹配，则为1，否则为0。</p><p>$<COMPILE_LANGUAGE><br>3.3新版功能。</p><p>计算编译选项时源文件的编译语言。关于这个生成器表达式的可移植性，请参阅相关的布尔表达式$ 。</p><p>$&lt;COMPILE_LANGUAGE:languages&gt;<br>3.3新版功能。</p><p>在3.15版更改:可以为语言指定多种语言。CMake 3.14及更早版本只接受单一语言。</p><p>当用于编译单元的语言与语言中任何以逗号分隔的条目匹配时，则为1，否则为0。此表达式可用于指定编译选项、编译定义，并在目标中包含特定语言的源文件的目录。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp main.cpp foo.c bar.cpp zot.cu<span class="token punctuation">)</span><span class="token keyword">target_compile_options</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CXX<span class="token punctuation">></span>:-fno-exceptions<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CXX<span class="token punctuation">></span>:COMPILING_CXX<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CUDA<span class="token punctuation">></span>:COMPILING_CUDA<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">target_include_directories</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CXX,CUDA<span class="token punctuation">></span>:/opt/foo/headers<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这指定了使用-fno-exceptions编译选项、COMPILING_CXX编译定义和cxx_headers include目录仅用于c++(编译器id检查省略)。它还为CUDA指定了COMPILING_CUDA编译定义。</p><p>注意，在Visual Studio Generators和Xcode中，没有办法表示目标范围的编译定义，也没有办法分别包含C和CXX语言的目录。而且，使用Visual Studio Generators，无法分别为C语言和CXX语言表示目标范围的标志。在这些生成器下，C和c++源的表达式如果有任何c++源，将使用CXX求值，否则使用C求值。一个解决方法是为每种源文件语言创建单独的库:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>myapp_c foo.c<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>myapp_cxx bar.cpp<span class="token punctuation">)</span><span class="token keyword">target_compile_options</span><span class="token punctuation">(</span>myapp_cxx <span class="token namespace">PUBLIC</span> -fno-exceptions<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp main.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myapp myapp_c myapp_cxx<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>$&lt;COMPILE_LANG_AND_ID:language,compiler_ids&gt;<br>3.15新版功能。</p><p>当编译单元使用的语言与language匹配，且语言编译器的CMake的编译器id与compiler_ids中任何一个以逗号分隔的条目匹配时，则为1，否则为0。这个表达式是$ 和$ 组合的简写形式。此表达式可用于指定编译选项、编译定义，并在目标中包含特定语言的源文件和编译器组合的目录。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp main.cpp foo.c bar.cpp zot.cu<span class="token punctuation">)</span><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANG_AND_ID:CXX,AppleClang,Clang<span class="token punctuation">></span>:COMPILING_CXX_WITH_CLANG<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANG_AND_ID:CXX,Intel<span class="token punctuation">></span>:COMPILING_CXX_WITH_INTEL<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>COMPILE_LANG_AND_ID:C,Clang<span class="token punctuation">></span>:COMPILING_C_WITH_CLANG<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这指定了基于编译器id和编译语言的不同编译定义的使用。当Clang是CXX编译器时，这个例子将有一个COMPILING_CXX_WITH_CLANG编译定义，当Intel是CXX编译器时，这个例子将有一个COMPILING_CXX_WITH_INTEL编译定义。同样，当C编译器是Clang时，它只能看到COMPILING_C_WITH_CLANG定义。</p><p>如果没有COMPILE_LANG_AND_ID生成器表达式，相同的逻辑将表示为:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_compile_definitions</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">AND</span>:<span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CXX<span class="token punctuation">></span>,<span class="token punctuation">$&lt;</span>CXX_COMPILER_ID:AppleClang,Clang<span class="token punctuation">></span><span class="token punctuation">></span>:COMPILING_CXX_WITH_CLANG<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">AND</span>:<span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:CXX<span class="token punctuation">></span>,<span class="token punctuation">$&lt;</span>CXX_COMPILER_ID:Intel<span class="token punctuation">></span><span class="token punctuation">></span>:COMPILING_CXX_WITH_INTEL<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span><span class="token operator">AND</span>:<span class="token punctuation">$&lt;</span>COMPILE_LANGUAGE:C<span class="token punctuation">></span>,<span class="token punctuation">$&lt;</span>C_COMPILER_ID:Clang<span class="token punctuation">></span><span class="token punctuation">></span>:COMPILING_C_WITH_CLANG<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="编译功能"><a href="#编译功能" class="headerlink" title="编译功能"></a>编译功能</h3><p>$&lt;COMPILE_FEATURES:features&gt;<br>3.1新版功能。</p><p>其中features是一个逗号分隔的列表。如果’head’目标的所有特征都可用，则返回1，否则返回0。如果在计算目标的链接实现时使用此表达式，并且如果任何依赖项传递性地增加了’head’目标所需的C_STANDARD或CXX_STANDARD，则会报告错误。有关编译特性的信息和支持的编译器列表，请参阅cmake-compile-features(7)手册。</p><h3 id="link语言和ID"><a href="#link语言和ID" class="headerlink" title="link语言和ID"></a>link语言和ID</h3><p>$<LINK_LANGUAGE><br>3.18新版功能。</p><p>计算链接选项时，目标的链接语言。请参阅相关的布尔表达式$ ，以了解该生成器表达式的可移植性。</p><p>请注意 链接库属性不支持此生成器表达式，以避免由于这些属性的双重求值而产生的副作用。</p><p>$&lt;LINK_LANGUAGE:languages&gt;<br>3.18新版功能。</p><p>当用于链接步骤的语言匹配语言中任何以逗号分隔的条目时，则为1，否则为0。此表达式可用于指定目标中特定语言的链接库、链接选项、链接目录和链接依赖项。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>api_C ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>api_CXX ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>api <span class="token namespace">INTERFACE</span><span class="token punctuation">)</span><span class="token keyword">target_link_options</span><span class="token punctuation">(</span>api   <span class="token namespace">INTERFACE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:C<span class="token punctuation">></span>:-opt_c<span class="token punctuation">></span>                                    <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:CXX<span class="token punctuation">></span>:-opt_cxx<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>api <span class="token namespace">INTERFACE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:C<span class="token punctuation">></span>:api_C<span class="token punctuation">></span>                                    <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:CXX<span class="token punctuation">></span>:api_CXX<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp1 main.c<span class="token punctuation">)</span><span class="token keyword">target_link_options</span><span class="token punctuation">(</span>myapp1 <span class="token namespace">PRIVATE</span> api<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp2 main.cpp<span class="token punctuation">)</span><span class="token keyword">target_link_options</span><span class="token punctuation">(</span>myapp2 <span class="token namespace">PRIVATE</span> api<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这指定使用api目标来链接目标myapp1和myapp2。实际上，myapp1将与目标api_C和选项-opt_c进行链接，因为它将使用C作为链接语言。myapp2将使用api_CXX和option -opt_cxx链接，因为CXX将是链接语言。</p><p>请注意 为了确定目标的链接语言，需要传递地收集将链接到它的所有目标。因此，对于链接库属性，将进行双重计算。在第一次求值期间，$&lt;LINK_LANGUAGE:..&gt; </p><p>表达式总是返回0。在第一次传递之后计算的链接语言将用于第二次传递。为了避免不一致，要求第二次传递不改变链接语言。此外，为了避免意外的副作用，需要指定完整的实体作为$ 表达式。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib <span class="token namespace">STATIC</span> file.cxx<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>libother <span class="token namespace">STATIC</span> file.c<span class="token punctuation">)</span><span class="token comment"># bad usage</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp1 main.c<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myapp1 <span class="token namespace">PRIVATE</span> lib<span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:C<span class="token punctuation">></span>:other<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token comment"># correct usage</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp2 main.c<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myapp2 <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANGUAGE:C<span class="token punctuation">></span>:libother<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在本例中，对于myapp1，第一次传递将意外地确定链接语言是CXX，因为生成器表达式的计算将是一个空字符串，因此myapp1将依赖于c++的目标库。相反，对于myapp2，第一次评估将给出C作为链接语言，因此第二次评估将正确地添加目标libother作为链接依赖项。</p><p>$&lt;LINK_LANG_AND_ID:language,compiler_ids&gt;<br>3.18新版功能。</p><p>当用于链接步骤的语言匹配language并且语言链接器的CMake编译器id匹配compiler_ids中任何一个逗号分隔的条目时，则为1，否则为0。该表达式是$ 和$ 组合的简写形式。此表达式可用于指定目标中特定语言和链接器组合的链接库、链接选项、链接目录和链接依赖项。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>libC_Clang ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>libCXX_Clang ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>libC_Intel ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>libCXX_Intel ...<span class="token punctuation">)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>myapp main.c<span class="token punctuation">)</span><span class="token keyword">if</span> <span class="token punctuation">(</span>CXX_CONFIG<span class="token punctuation">)</span>  <span class="token keyword">target_sources</span><span class="token punctuation">(</span>myapp <span class="token namespace">PRIVATE</span> file.cxx<span class="token punctuation">)</span><span class="token keyword">endif</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>myapp  <span class="token namespace">PRIVATE</span> <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANG_AND_ID:CXX,Clang,AppleClang<span class="token punctuation">></span>:libCXX_Clang<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANG_AND_ID:C,Clang,AppleClang<span class="token punctuation">></span>:libC_Clang<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANG_AND_ID:CXX,Intel<span class="token punctuation">></span>:libCXX_Intel<span class="token punctuation">></span>          <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>LINK_LANG_AND_ID:C,Intel<span class="token punctuation">></span>:libC_Intel<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这指定了基于编译器id和链接语言的不同链接库的使用。当Clang或AppleClang是CXX链接器时，这个例子将把目标libCXX_Clang作为链接依赖项，当Intel是CXX链接器时，将把目标libCXX_Intel作为链接依赖项。同样地，当C链接器是Clang或AppleClang时，目标libC_Clang将被添加为链接依赖项，当Intel是C链接器时，目标libC_Intel将被添加为链接依赖项。</p><p>有关使用此生成器表达式的约束，请参阅与$ 相关的说明。</p><h3 id="链接功能"><a href="#链接功能" class="headerlink" title="链接功能"></a>链接功能</h3><p>$&lt;LINK_LIBRARY:feature,library-list&gt;<br>3.24新版功能。</p><p>指定一组要链接到目标的库，以及提供关于应该如何链接它们的详细信息的特性。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 <span class="token namespace">STATIC</span> ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 ...<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib2 <span class="token namespace">PRIVATE</span> <span class="token string">"$&lt;LINK_LIBRARY:WHOLE_ARCHIVE,lib1>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这指定lib2应该链接到lib1，并在这样做时使用WHOLE_ARCHIVE特性。</p><p>特性名称区分大小写，只能包含字母、数字和下划线。所有大写的特性名称都保留给CMake自己的内置特性。预定义的内置库特性包括:</p><p>默认的<br>这个特性对应于标准链接，本质上等同于不使用任何特性。它通常只与LINK_LIBRARY_OVERRIDE和link_library_overrides 目标属性一起使用。</p><p>WHOLE_ARCHIVE<br>强制包含静态库的所有成员。此功能仅支持以下平台，其局限性如下所示:</p><p>Linux。</p><p>所有BSD变体。</p><p>SunOS。</p><p>所有苹果的变种。库必须指定为CMake目标名称、库文件名(如libfoo.a)或库文件路径(如/path/to/libfoo.a)。由于Apple链接器的限制，它不能被指定为普通的库名，比如foo，其中foo不是CMake目标。</p><p>窗户当使用MSVC或类似MSVC的工具链时，MSVC版本必须大于1900。</p><p>Cygwin。</p><p>MSYS。</p><p>框架<br>该选项告诉链接器使用-framework linker选项搜索指定的框架。它只能在苹果平台上使用，并且只能使用一个理解所使用选项的链接器(即Xcode提供的链接器，或与之兼容的链接器)。</p><p>该框架可以指定为CMake框架目标、裸框架名称或文件路径。如果给出了一个目标，该目标必须将FRAMEWORK target属性设置为true。对于文件路径，如果它包含目录部分，则该目录将被添加为框架搜索路径。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib <span class="token namespace">SHARED</span> ...<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib <span class="token namespace">PRIVATE</span> <span class="token string">"$&lt;LINK_LIBRARY:FRAMEWORK,/path/to/my_framework>"</span><span class="token punctuation">)</span><span class="token comment"># The constructed linker command line will contain:</span><span class="token comment">#   -F/path/to -framework my_framework</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>文件路径必须符合以下模式之一(*为通配符，可选部分显示为[…]):</p><p>[/路径/ /]FwName [.framework]</p><p>[/路径/ /]FwName.framework / FwName(后缀)</p><p>[/路径/ /]FwName.framework /版本/ * / FwName(后缀)</p><p>注意，CMake识别并自动处理框架目标，即使不使用$ 表达式。生成器表达式仍然可以与CMake目标一起使用，如果项目想显式地使用它，但并不要求这样做。链接器命令行在使用生成器表达式或不使用生成器表达式之间可能有一些差异，但最终结果应该是相同的。另一方面，如果给出了文件路径，CMake会自动识别一些路径，但不是所有情况。项目可能需要使用$ 的文件路径，以便预期的行为是明确的。</p><p>3.25新版功能:框架特性现在支持FRAMEWORK_MULTI_CONFIG_POSTFIX_ 目标属性以及框架库名称的后缀。</p><p>NEEDED_FRAMEWORK<br>这类似于框架特性，除了它强制链接器链接到框架，即使没有从框架中使用符号。它使用- neede_framework选项，并具有与FRAMEWORK相同的链接器约束。</p><p>REEXPORT_FRAMEWORK<br>这类似于FRAMEWORK特性，除了它告诉链接器框架应该对链接到正在创建的库的客户端可用。它使用-reexport_framework选项，并具有与FRAMEWORK相同的链接器约束。</p><p>WEAK_FRAMEWORK<br>这类似于FRAMEWORK特性，除了它强制链接器将框架及其所有引用标记为弱导入。它使用-weak_framework选项，并具有与FRAMEWORK相同的链接器约束。</p><p>NEEDED_LIBRARY<br>这类似于NEEDED_FRAMEWORK特性，除了它用于非框架目标或库(仅限苹果平台)。它适当地使用- neede_library或-needed-l选项，并具有与neede_framework相同的链接器约束。</p><p>REEXPORT_LIBRARY<br>这类似于REEXPORT_FRAMEWORK特性，除了它用于非框架目标或库(仅限苹果平台)。它适当地使用-reexport_library或-reexport-l选项，并具有与REEXPORT_FRAMEWORK相同的链接器约束。</p><p>WEAK_LIBRARY<br>这类似于WEAK_FRAMEWORK特性，除了它用于非框架目标或库(仅限苹果平台)。它适当地使用-weak_library或-weak-l选项，并具有与WEAK_FRAMEWORK相同的链接器约束。</p><p>内置和自定义库特性是根据以下变量定义的:</p><p>CMAKE_&lt;LANG&gt;_LINK_LIBRARY_USING_&lt;FEATURE&gt;_SUPPORTED</p><p>CMAKE_&lt;LANG&gt;_LINK_LIBRARY_USING_&lt;FEATURE&gt;</p><p>CMAKE_LINK_LIBRARY_USING_&lt;FEATURE&gt;_SUPPORTED</p><p>CMAKE_LINK_LIBRARY_USING_&lt;FEATURE&gt;<br>用于每个变量的值是在创建目标的目录作用域的末尾设置的值。用法如下:</p><p>如果特定于语言的CMAKE_ <em>LINK_LIBRARY_USING</em> <em>SUPPORTED变量为真，则该特性必须由相应的CMAKE</em> <em>LINK_LIBRARY_USING</em> 变量定义。</p><p>如果不支持特定于语言的特性，则CMAKE_LINK_LIBRARY_USING_&lt; feature &gt;_SUPPORTED变量必须为真，并且该特性必须由相应的CMAKE_LINK_LIBRARY_USING_&lt; feature &gt;变量定义。</p><p>应注意以下限制:</p><p>库列表可以指定CMake目标或库。任何OBJECT或INTERFACE类型的CMake目标都将忽略表达式的特征方面，而是以标准方式链接。</p><p>$ &lt; LINK_LIBRARY:……&gt;<br>生成器表达式只能用于指定链接库。实际上，这意味着它可以出现在LINK_LIBRARIES、INTERFACE_LINK_LIBRARIES和INTERFACE_LINK_LIBRARIES_DIRECT目标属性中，并在target_link_libraries()和LINK_LIBRARIES()命令中指定。</p><p>如果$&lt;LINK_LIBRARY:…&gt;生成器表达式出现在目标的INTERFACE_LINK_LIBRARIES属性中，它将包含在由安装(EXPORT)命令生成的导入目标中。使用此导入的环境负责定义此表达式使用的链接特性。</p><p>链接步骤中涉及的每个目标或库最多只能有一种库特性。一个特性的缺失也与所有其他特性不兼容。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib3 ...<span class="token punctuation">)</span><span class="token comment"># lib1 will be associated with feature1</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib2 <span class="token namespace">PUBLIC</span> <span class="token string">"$&lt;LINK_LIBRARY:feature1,lib1>"</span><span class="token punctuation">)</span><span class="token comment"># lib1 is being linked with no feature here. This conflicts with the</span><span class="token comment"># use of feature1 in the line above and would result in an error.</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib3 <span class="token namespace">PRIVATE</span> lib1 lib2<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果不可能在整个构建过程中对给定的目标或库使用相同的特性，则可以使用LINK_LIBRARY_OVERRIDE和link_library_overrides &lt; library &gt;目标属性来解决此类不兼容性问题。</p><p>$ &lt; LINK_LIBRARY:……&gt;<br>生成器表达式不保证指定目标和库的列表将保持分组在一起。要像GNU ld链接器所支持的那样管理——start-group和——end-group这样的构造，请使用LINK_GROUP生成器表达式。</p><p>$&lt;LINK_GROUP:feature,library-list&gt;<br>3.24新版功能。</p><p>指定要链接到目标的库组，以及定义该组应如何链接的特性。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 <span class="token namespace">STATIC</span> ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 ...<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib2 <span class="token namespace">PRIVATE</span> <span class="token string">"$&lt;LINK_GROUP:RESCAN,lib1,external>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>这指定lib2应该链接到lib1和外部库，并且根据RESCAN特性的定义，这两个库都应该包含在链接器命令行中。</p><p>特性名称区分大小写，只能包含字母、数字和下划线。所有大写的特性名称都保留给CMake自己的内置特性。目前，只有一个预定义的内置组特性:</p><p>重新扫描<br>有些链接器只能单次通过。对于这样的链接器，库之间的循环引用通常会导致无法解析的符号。该特性指示链接器重复搜索指定的静态库，直到没有新的未定义引用被创建为止。</p><p>通常，静态库只按照在命令行中指定的顺序搜索一次。如果需要该库中的符号来解析稍后出现在命令行上的库中对象引用的未定义符号，则链接器将无法解析该引用。通过使用RESCAN特性对静态库进行分组，将重复搜索它们，直到解析所有可能的引用。这将使用链接器选项，如——start-group和——end-group，或者在SunOS上，-z rescan-start和-z rescan-end。</p><p>使用这个特性会带来很大的性能成本。最好只在两个或多个静态库之间存在不可避免的循环引用时使用它。</p><p>该特性在使用针对Linux、BSD和SunOS的工具链时可用。如果使用GNU工具链，它也可以用于Windows平台。</p><p>内置和自定义组功能是根据以下变量定义的:</p><p>CMAKE_&lt;LANG&gt;_LINK_GROUP_USING_&lt;FEATURE&gt;_SUPPORTED</p><p>CMAKE_&lt;LANG&gt;_LINK_GROUP_USING_&lt;FEATURE&gt;</p><p>CMAKE_LINK_GROUP_USING_&lt;FEATURE&gt;_SUPPORTED</p><p>CMAKE_LINK_GROUP_USING_&lt;FEATURE&gt;<br>用于每个变量的值是在创建目标的目录作用域的末尾设置的值。用法如下:</p><p>如果特定于语言的CMAKE_ <em>LINK_GROUP_USING</em> <em>SUPPORTED变量为真，则该特性必须由相应的CMAKE</em> <em>LINK_GROUP_USING</em> 变量定义。</p><p>如果不支持特定于语言的特性，则CMAKE_LINK_GROUP_USING_&lt; feature &gt;<em>SUPPORTED变量必须为真，并且该特性必须由相应的CMAKE_LINK_GROUP_USING</em>&lt; feature &gt;变量定义。</p><p>LINK_GROUP生成器表达式与LINK_LIBRARY生成器表达式兼容。可以使用LINK_LIBRARY生成器表达式指定组中涉及的库。</p><p>链接步骤中涉及的每个目标或外部库都可以是多个组的一部分，但前提是所有涉及的组都指定了相同的特性。这样的组不会在链接器命令行上被合并，单独的组仍然会被保留。禁止为相同的目标或库混合不同的组特征。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib3 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib4 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib5 ...<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib3 <span class="token namespace">PUBLIC</span>  <span class="token string">"$&lt;LINK_GROUP:feature1,lib1,lib2>"</span><span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib4 <span class="token namespace">PRIVATE</span> <span class="token string">"$&lt;LINK_GROUP:feature1,lib1,lib3>"</span><span class="token punctuation">)</span><span class="token comment"># lib4 will be linked with the groups &#123;lib1,lib2&#125; and &#123;lib1,lib3&#125;.</span><span class="token comment"># Both groups specify the same feature, so this is fine.</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib5 <span class="token namespace">PRIVATE</span> <span class="token string">"$&lt;LINK_GROUP:feature2,lib1,lib3>"</span><span class="token punctuation">)</span><span class="token comment"># An error will be raised here because both lib1 and lib3 are part of two</span><span class="token comment"># groups with different features.</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当目标或外部库作为组的一部分参与链接步骤，同时又不属于任何组时，任何出现的非组链接项都将被它所属的组替换。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib3 ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib4 ...<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib3 <span class="token namespace">PUBLIC</span> lib1<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib4 <span class="token namespace">PRIVATE</span> lib3 <span class="token string">"$&lt;LINK_GROUP:feature1,lib1,lib2>"</span><span class="token punctuation">)</span><span class="token comment"># lib4 will only be linked with lib3 and the group &#123;lib1,lib2&#125;</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>因为lib1是为lib4定义的组的一部分，所以这个组将应用回对lib3使用lib1。最终结果就像lib3的链接关系被指定为:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib3 <span class="token namespace">PUBLIC</span> <span class="token string">"$&lt;LINK_GROUP:feature1,lib1,lib2>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>注意，组相对于非组链接项的优先级可能导致组之间的循环依赖关系。如果发生这种情况，将引发致命错误，因为不允许组使用循环依赖项。</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1A ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib1B ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2A ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib2B ...<span class="token punctuation">)</span><span class="token keyword">add_library</span><span class="token punctuation">(</span>lib3 ...<span class="token punctuation">)</span><span class="token comment"># Non-group linking relationships, these are non-circular so far</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib1A <span class="token namespace">PUBLIC</span> lib2A<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib2B <span class="token namespace">PUBLIC</span> lib1B<span class="token punctuation">)</span><span class="token comment"># The addition of these groups creates circular dependencies</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib3 <span class="token namespace">PRIVATE</span>  <span class="token string">"$&lt;LINK_GROUP:feat,lib1A,lib1B>"</span>  <span class="token string">"$&lt;LINK_GROUP:feat,lib2A,lib2B>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>由于为lib3定义了组，lib1A和lib2B的链接关系有效地扩展为等价的:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib1A <span class="token namespace">PUBLIC</span> <span class="token string">"$&lt;LINK_GROUP:feat,lib2A,lib2B>"</span><span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>lib2B <span class="token namespace">PUBLIC</span> <span class="token string">"$&lt;LINK_GROUP:feat,lib1A,lib1B>"</span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>这在组之间创建了一个循环依赖:lib1A—&gt; lib2B—&gt; lib1A。</p><p>还应注意以下限制:</p><p>库列表可以指定CMake目标或库。任何OBJECT或INTERFACE类型的CMake目标都将忽略表达式的特征方面，而是以标准方式链接。</p><p>$ &lt; LINK_GROUP:……&gt;生成器表达式只能用于指定链接库。实际上，这意味着它可以出现在LINK_LIBRARIES、INTERFACE_LINK_LIBRARIES和INTERFACE_LINK_LIBRARIES_DIRECT目标属性中，并在target_link_libraries()和LINK_LIBRARIES()命令中指定。</p><p>如果&lt; LINK_GROUP:……&gt;生成器表达式出现在目标的INTERFACE_LINK_LIBRARIES属性中，它将包含在由安装(EXPORT)命令生成的导入目标中。使用此导入的环境负责定义此表达式使用的链接特性。</p><h3 id="联系上下文"><a href="#联系上下文" class="headerlink" title="联系上下文"></a>联系上下文</h3><p>$&lt;LINK_ONLY:…&gt;<br>3.1新版功能。</p><p>的内容……，但在收集传递使用需求时除外，在这种情况下，它是空字符串。这用于INTERFACE_LINK_LIBRARIES目标属性中，通常通过target_link_libraries()命令填充，以指定私有链接依赖关系，而不需要其他使用要求。</p><p>3.24新版功能:LINK_ONLY也可以在LINK_LIBRARIES目标属性中使用。参见策略CMP0131。</p><p>$&lt;DEVICE_LINK:list&gt;<br>3.18新版功能。</p><p>如果是设备链接步骤，则返回列表，否则返回空列表。设备链接步骤由CUDA_SEPARABLE_COMPILATION和CUDA_RESOLVE_DEVICE_SYMBOLS属性和策略CMP0105控制。此表达式只能用于指定链接选项。</p><p>$&lt;HOST_LINK:list&gt;<br>3.18新版功能。</p><p>如果是普通的链接步骤，则返回列表，否则返回空列表。当还涉及到设备链接步骤时，此表达式主要有用(请参阅$ 生成器表达式)。此表达式只能用于指定链接选项。</p><h3 id="依赖于目标的表达式"><a href="#依赖于目标的表达式" class="headerlink" title="依赖于目标的表达式"></a>依赖于目标的表达式</h3><p>这些查询引用目标tgt。除非另有说明，这可以是任何运行时工件，即:</p><p>由add_executable()创建的可执行目标。</p><p>共享库目标(。因此，.dll而不是他们的.lib导入库)由add_library()创建。</p><p>由add_library()创建的静态库目标。</p><p>在下文中，短语“tgt文件名”指的是tgt二进制文件的名称。这必须与短语“目标名称”区别开来，后者只是字符串tgt。</p><p>$&lt;TARGET_EXISTS:tgt&gt;<br>3.12新版功能。</p><p>如果tgt作为CMake目标存在，则为1，否则为0。</p><p>$&lt;TARGET_NAME_IF_EXISTS:tgt&gt;<br>3.12新版功能。</p><p>如果目标存在，则目标名tgt，否则为空字符串。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_NAME:…&gt;<br>标志着……作为目标的名字。如果将目标导出到多个依赖的导出集，这是必需的。…必须是目标的字面名称，它不能包含生成器表达式。</p><p>$&lt;TARGET_PROPERTY:tgt,prop&gt;<br>目标tgt上的属性道具的值。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_PROPERTY:prop&gt;<br>属性prop在表达式被求值的目标上的值。注意，对于传递使用需求中的生成器表达式，这是消费目标，而不是指定需求的目标。</p><p>$&lt;TARGET_OBJECTS:tgt&gt;<br>3.1新版功能。</p><p>生成tgt产生的对象列表。这通常用于对象库目标。</p><p>$&lt;TARGET_POLICY:policy&gt;<br>如果创建’head’目标时策略为NEW，则为1，否则为0。如果没有设置策略，则会发出策略的警告消息。此生成器表达式仅适用于策略的一个子集。</p><p>$&lt;TARGET_FILE:tgt&gt;<br>tgt二进制文件的完整路径。</p><p>请注意，tgt并不是作为计算该表达式的目标的依赖项添加的，除非该表达式在add_custom_command()或add_custom_target()中使用。</p><p>$&lt;TARGET_FILE_BASE_NAME:tgt&gt;<br>3.15新版功能。</p><p>tgt的基本名称，即$&lt;TARGET_FILE_NAME:tgt&gt; ，不带前缀和后缀。例如，如果tgt文件名是libbase。基名是base。</p><p>另请参阅OUTPUT_NAME、ARCHIVE_OUTPUT_NAME、LIBRARY_OUTPUT_NAME和RUNTIME_OUTPUT_NAME目标属性及其特定于配置的变体OUTPUT_NAME_ 、ARCHIVE_OUTPUT_NAME_ 、LIBRARY_OUTPUT_NAME_ 和RUNTIME_OUTPUT_NAME_ 。</p><p>也可以考虑 _POSTFIX和DEBUG_POSTFIX目标属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_FILE_PREFIX:tgt&gt;<br>3.15新版功能。</p><p>tgt文件名的前缀(例如lib)。</p><p>请参见PREFIX target属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_FILE_SUFFIX:tgt&gt;<br>3.15新版功能。</p><p>tgt文件名的后缀(扩展名如.so或.exe)。</p><p>请参见SUFFIX target属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_FILE_NAME:tgt&gt;<br>tgt文件名。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_FILE_DIR:tgt&gt;<br>tgt二进制文件的目录。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_LINKER_FILE:tgt&gt;<br>链接到tgt目标时使用的文件。这通常是tgt表示的库(。a， .lib， .so)，但对于DLL平台上的共享库，它将是与DLL关联的.lib导入库。</p><p>$&lt;TARGET_LINKER_FILE_BASE_NAME:tgt&gt;<br>3.15新版功能。</p><p>用于链接目标tgt的基本文件名，例如$ ，不带前缀和后缀。例如，目标文件名为libbase。一、基地名称为基地。</p><p>另请参阅OUTPUT_NAME、ARCHIVE_OUTPUT_NAME和LIBRARY_OUTPUT_NAME目标属性及其特定于配置的变体OUTPUT_NAME_ 、ARCHIVE_OUTPUT_NAME_ 和LIBRARY_OUTPUT_NAME_ 。</p><p>也可以考虑 _POSTFIX和DEBUG_POSTFIX目标属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_LINKER_FILE_PREFIX:tgt&gt;<br>3.15新版功能。</p><p>用于链接目标tgt的文件前缀。</p><p>另请参阅PREFIX和IMPORT_PREFIX目标属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_LINKER_FILE_SUFFIX:tgt&gt;<br>3.15新版功能。</p><p>用于链接的文件后缀，其中tgt是目标的名称。</p><p>后缀对应于文件扩展名(例如“。所以”或”.lib”)。</p><p>另请参阅SUFFIX和IMPORT_SUFFIX目标属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_LINKER_FILE_NAME:tgt&gt;<br>用于链接目标tgt的文件名。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_LINKER_FILE_DIR:tgt&gt;<br>用于链接目标tgt的文件目录。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_SONAME_FILE:tgt&gt;<br>带有soname (.so.3)的文件，其中tgt是目标的名称。</p><p>$&lt;TARGET_SONAME_FILE_NAME:tgt&gt;<br>带有soname (.so.3)的文件名。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_SONAME_FILE_DIR:tgt&gt;<br>使用soname (.so.3)的目录。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_PDB_FILE:tgt&gt;<br>3.1新版功能。</p><p>链接器生成的程序数据库文件(.pdb)的完整路径，其中tgt是目标的名称。</p><p>另请参阅PDB_NAME和PDB_OUTPUT_DIRECTORY目标属性及其特定于配置的变体PDB_NAME 和PDB_OUTPUT_DIRECTORY_ 。</p><p>$&lt;TARGET_PDB_FILE_BASE_NAME:tgt&gt;<br>3.15新版功能。</p><p>链接器生成的程序数据库文件(.pdb)的基本名称，其中tgt是目标的名称。</p><p>基本名称对应于不带前缀和后缀的目标PDB文件名(参见$ )。例如，如果目标文件名是base。Pdb，基名为base。</p><p>另请参阅PDB_NAME目标属性及其特定于配置的变体PDB_NAME 。</p><p>也可以考虑 _POSTFIX和DEBUG_POSTFIX目标属性。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加。</p><p>$&lt;TARGET_PDB_FILE_NAME:tgt&gt;<br>3.1新版功能。</p><p>链接器生成的程序数据库文件(.pdb)的名称。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_PDB_FILE_DIR:tgt&gt;<br>3.1新版功能。</p><p>链接器生成的程序数据库文件(.pdb)的目录。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_BUNDLE_DIR:tgt&gt;<br>3.9新版功能。</p><p>bundle目录的完整路径(/path/to/my.)App， /path/to/my.framework，或/path/to/my.bundle)，其中TGT是目标的名称。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_BUNDLE_DIR_NAME:tgt&gt;<br>3.24新版功能。</p><p>包目录的名称(my。App、my.framework或my.bundle)，其中TGT是目标的名称。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_BUNDLE_CONTENT_DIR:tgt&gt;<br>3.9新版功能。</p><p>包内容目录的完整路径，其中tgt是目标的名称。对于macOS SDK，它指向/path/to/my。app/Contents， /path/to/my.framework，或/path/to/my.bundle/Contents。对于所有其他sdk(例如iOS)，它指向/path/to/my。App， /path/to/my.framework，或/path/to/my.framework。由于扁平的束结构。</p><p>请注意，tgt并没有作为计算该表达式的目标的依赖项添加(请参阅策略CMP0112)。</p><p>$&lt;TARGET_RUNTIME_DLLS:tgt&gt;<br>3.21新版功能。</p><p>目标在运行时依赖的dll列表。这是由目标的传递依赖项中所有SHARED目标的位置决定的。在可执行文件、SHARED库和MODULE库以外的目标上使用此生成器表达式是错误的。在非dll平台上，这个表达式总是求值为空字符串。</p><p>此生成器表达式可用于将目标所依赖的所有dll复制到POST_BUILD自定义命令的输出目录中。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">find_package</span><span class="token punctuation">(</span>foo CONFIG REQUIRED<span class="token punctuation">)</span> <span class="token comment"># package generated by install(EXPORT)</span><span class="token keyword">add_executable</span><span class="token punctuation">(</span>exe main.c<span class="token punctuation">)</span><span class="token keyword">target_link_libraries</span><span class="token punctuation">(</span>exe <span class="token namespace">PRIVATE</span> <span class="token inserted class-name">foo::foo</span> <span class="token inserted class-name">foo::bar</span><span class="token punctuation">)</span><span class="token keyword">add_custom_command</span><span class="token punctuation">(</span>TARGET exe POST_BUILD  COMMAND <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_COMMAND</span><span class="token punctuation">&#125;</span> -E copy <span class="token punctuation">$&lt;</span>TARGET_RUNTIME_DLLS:exe<span class="token punctuation">></span> <span class="token punctuation">$&lt;</span>TARGET_FILE_DIR:exe<span class="token punctuation">></span>  COMMAND_EXPAND_LISTS<span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>请注意 导入的目标只有在知道其.dll文件的位置时才会被支持。导入的SHARED库必须将IMPORTED_LOCATION设置为其.dll文件。有关详细信息，请参阅add_library导入的库部分。许多查找模块生成UNKNOWN类型的导入目标，因此将被忽略。</p><h3 id="导出和安装表达式"><a href="#导出和安装表达式" class="headerlink" title="导出和安装表达式"></a>导出和安装表达式</h3><p>$&lt;INSTALL_INTERFACE:…&gt;<br>的内容……当使用install(EXPORT)导出属性时，否则为空。</p><p>$&lt;BUILD_INTERFACE:…&gt;<br>的内容……当使用export()导出属性时，或者当目标被同一构建系统中的另一个目标使用时。否则展开为空字符串。</p><p>$<INSTALL_PREFIX><br>当通过install(EXPORT)导出目标时，或在INSTALL_NAME_DIR属性或install(RUNTIME_DEPENDENCY_SET)的INSTALL_NAME_DIR参数中求值时，安装前缀的内容，否则为空。</p><h3 id="多层次表达评价"><a href="#多层次表达评价" class="headerlink" title="多层次表达评价"></a>多层次表达评价</h3><p>$&lt;GENEX_EVAL:expr&gt;<br>3.12新版功能。</p><p>在当前上下文中作为生成器表达式计算的expr的内容。这允许使用生成器表达式，其计算结果本身就是生成器表达式。</p><p>$&lt;TARGET_GENEX_EVAL:tgt,expr&gt;<br>3.12新版功能。</p><p>expr的内容在tgt目标的上下文中作为生成器表达式计算。这允许使用本身包含生成器表达式的自定义目标属性。</p><p>当您希望管理支持生成器表达式的自定义属性时，具有计算生成器表达式的功能非常有用。例如:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_library</span><span class="token punctuation">(</span>foo ...<span class="token punctuation">)</span><span class="token keyword">set_property</span><span class="token punctuation">(</span>TARGET foo PROPERTY  CUSTOM_KEYS <span class="token punctuation">$&lt;</span><span class="token punctuation">$&lt;</span>CONFIG:DEBUG<span class="token punctuation">></span>:FOO_EXTRA_THINGS<span class="token punctuation">></span><span class="token punctuation">)</span><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>printFooKeys  COMMAND <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_COMMAND</span><span class="token punctuation">&#125;</span> -E echo <span class="token punctuation">$&lt;</span>TARGET_PROPERTY:foo,CUSTOM_KEYS<span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>printFooKeys自定义命令的简单实现是错误的，因为CUSTOM_KEYS目标属性没有被计算，并且内容按原样传递(即$&lt;$ :FOO_EXTRA_THINGS&gt;)。</p><p>为了得到预期的结果(例如，如果config是Debug，则FOO_EXTRA_THINGS)，需要计算$ 的输出:</p><pre class="line-numbers language-cmake" data-language="cmake"><code class="language-cmake"><span class="token keyword">add_custom_target</span><span class="token punctuation">(</span>printFooKeys  COMMAND <span class="token punctuation">$&#123;</span><span class="token variable">CMAKE_COMMAND</span><span class="token punctuation">&#125;</span> -E    echo <span class="token punctuation">$&lt;</span>TARGET_GENEX_EVAL:foo,<span class="token punctuation">$&lt;</span>TARGET_PROPERTY:foo,CUSTOM_KEYS<span class="token punctuation">></span><span class="token punctuation">></span><span class="token punctuation">)</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h3 id="escaped-characaters"><a href="#escaped-characaters" class="headerlink" title="escaped characaters"></a>escaped characaters</h3><p>这些表达式求值为特定的字符串字面量。使用它们来代替实际的字符串字面量，以防止它们具有特殊的含义。</p><p>$&lt;ANGLE-R&gt;<br>一个字面的&gt;。例如，用于比较包含&gt;的字符串。</p><p>$&lt;COMMA&gt;<br>字面上的，。例如，用于比较包含，的字符串。</p><p>$&lt;SEMICOLON&gt;<br>字面上的;用于防止对参数进行列表扩展。</p><h3 id="弃用表达式"><a href="#弃用表达式" class="headerlink" title="弃用表达式"></a>弃用表达式</h3><p>$&lt;CONFIGURATION&gt;<br>配置名称。CMake 3.0后已弃用。使用CONFIG代替。</p>]]></content>
      
      
      <categories>
          
          <category> 编译安装 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> c++ </tag>
            
            <tag> c </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
